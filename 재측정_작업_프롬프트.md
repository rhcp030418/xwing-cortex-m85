# A주제 실험 재점검 — 재측정 작업 프롬프트

> 자체 완결 문서. 새 세션에 이 파일 하나만 줘도 실행 가능하도록 씀.
> 작성 2026-08-24. 근거: 2026-08-24 실험 기록 전수 재점검(로그 5회 대조 + 커널 파일 타임스탬프).
> **상태: 실행 완료 (2026-08-24).** AF/AG/AI 각 독립 2회와 정확성 게이트를 완료했고,
> 결과는 `measure-harness/2026-08-24_exp{AF_pilot_rebaseline,AG,AI}_results.md`에 보존했다.
> 범위는 A주제 실험 원천 문서만 — `paper/`·`A주제_발표자료.html`은 손대지 않았다.
> 단 A-3의 “5개 원시 로그” 중 expT 파일은 T1 행이 없는 요약본이어서, 보관 원시자료로
> 직접 재확인 가능한 것은 4개다. 이를 결과처럼 보완하지 않고 문서에 **미확인**으로 남겼다.

---

## 1. 임무

08-24 재점검에서 나온 불일치를 닫는다. 두 종류다.

- **파트 A — 측정 불필요 3건**: 이미 보관된 로그가 답을 주므로 문서만 정정.
- **파트 B — 재측정 3건**: ① 파일럿 슬롯(실험 4~7) 정본 재확정(AF), ② 현재 재료로 솔버 vs 지퍼 재대결(AG), ③ AE2 encaps 불균형 분해(AI).

## 2. 원칙 (AGENTS.md + 이 프로젝트 관행)

1. 가정은 탐색용. 결론은 실빌드·KAT·실보드 측정으로만.
2. 미측정은 반드시 `가정`/`추정`/`미측정` 표기.
3. **측정 전에 사전등록**: `measure-harness/2026-08-XX_exp{이름}_prereg.md`에 질문·구현·측정 방법·정확성 게이트·판정 기준을 먼저 적고 시작한다(기존 prereg 파일 형식 따라).
4. 모든 수치는 **독립 플래시/실행 2회** 재현 + KAT/mismatch/`harness_fails=0` 통과분만 채택.
5. 기존 수치는 지우지 않는다 — **정정 블록**으로 이력을 남긴다(리포트 v1.x 정정 블록 관례). 편집 전 `파일.bak-20260824` 백업.
6. `1단계_게이트_리포트.md` §1~§4·§6은 **동결부** — 수정 금지. §5·§7(파일럿 일지)만 갱신 가능.
7. md/py 파일은 PowerShell 리다이렉트로 쓰지 말 것(CP949 깨짐) — 에디터/Write 도구로.

## 3. 측정 파이프라인 (확립된 절차)

- 보드: EK-RA8M1 (Cortex-M85, 480MHz), J-Link. e2 studio 디버그 세션은 닫은 상태에서:

```powershell
# 1) 헤드리스 빌드 (새 파일 추가 시 -build 대신 -cleanBuild)
& "C:\Renesas\RA\e2studio_v2026-04.2_fsp_v6.5.0\eclipse\e2studioc.exe" -nosplash `
  -application org.eclipse.cdt.managedbuilder.core.headlessbuild `
  -data <임시워크스페이스> -build "blinky/Debug"
# 2) 플래시 → 실행 → 로그 회수
& "C:\Users\cnscj\AppData\Local\Programs\Python\Python314\python.exe" harvest_ra8m1.py
```

- 프로젝트: `C:\Users\cnscj\e2_studio\workspace\blinky` (src/harness.c, src/gen/). 회수기는 `A주제/measure-harness/`(실험별 `harvest_ra8m1_exp*.py` 변형 관례).
- 측정 규약: DWT CYCCNT, **N=100 중앙값, calib 25 차감**, 코드 ITCM·데이터/스택 DTCM, 통합 연산 비교는 ABBA. 루프 헤드 `.balign 16`(정렬에 ±1 cyc/iter 민감).
- 로그는 `measure-harness/logs/2026-08-XX_exp{이름}_run{1,2}.txt`로 보존.
- SLOTHY(AG용): WSL2 venv, `slothy-2stream/patch_slothy_v81m.py` v0.2 경로. 전체 규모 솔브는 CP-SAT 수 시간 소요 — 백그라운드로 돌리고 기다릴 것.

## 4. 배경 — 무엇이 어긋나 있나 (08-24 재점검 결과)

**핵심 사실**: 파일럿 커널이 08-16 오전 측정 **이후** 재생성됐다(`e2_studio/.../src/gen/stitched.s` 08-16 19:51, `mul256_flat.s` 08-17 02:14; `mul256_flat6.s`만 08-16 05:05 원본). 로드맵 9~17차에 재측정 기록 없음. 이후 보관 로그 5회(08-21 expQ·expJsolo·expT + 08-24 AE2 run1/2)는 서로 일치하되 08-16 기록과 다르다:

| 슬롯 | §7 기록(08-16) | 08-21~24 재측정 5회 | 차이 |
|---|---:|---:|---:|
| mul256 직선형 단독 | 397 | 377~378 | −20 |
| θ 단독 / 곱×θ 스티치 | 88 / 413 | 70 / 396 | −18 / −17 |
| 라운드 v4 / 곱×라운드(v0.3) | 678 / 799 | 659 / 781 | −19 / −18 |
| round6 / **mul6(flat6)** | 416 / **395** | 397 / **395** | −19 / **0** |
| 곱×round6 스티치(v6) | 681 | 658.4(08-21)~662.4(08-24) | −19~−23 |
| k=2 순차/스티치 | 1,206 / 1,019 | 1,186 / 1,001 | −18 |
| bfull 순차/솔버(08-16 B 내장) | 727 / 657 | 726 / 656 | −1 |

파급 2건: ① 실험 5 슬롯 회계 "87% 소진"이 현재 값으로 불성립(A 유휴 2×377.5−565=190 < 절감 256). ② T2 "지퍼 681 vs 솔버 657(−3.5%)"은 08-16 동세대끼리만 유효 — 현재 펌웨어에선 지퍼 658~662 vs 솔버 656(−0.4~−1.0%). 단 솔버(bfull)는 08-16 B를 내장하고 있어 이후 B 개선분을 못 받았다 → 재대결 필요(파트 B-AG).

기타 확정 오류: T2 "지퍼 1,018"(로그 5회 전부 지퍼=1,013·v0.4 솔버=1,018), X-Wing 리포트 14행 "326.5 = yield2"(실제 yield3), T4-b "+12.1%(→323.6)"(빌드 혼합 쌍 — 실측 세금은 펌웨어별 +12.1~12.9%).

## 5. 파트 A — 측정 없이 마감 (문서 정정 3건)

각 항목: 백업 → 정정 블록으로 수정 → 근거 로그를 파일:행까지 병기.

**A-1. T2 지퍼/솔버 행** — `논문_데이터_표.md:80`, `:319`(기타 "F-solver 1차 실패"), 로드맵 해당 서술.
지퍼 1,018 → **1,013**, 솔버 vs 지퍼 −2.2% → **−1.7%**, 은닉 57→60% → **58→60%**. 1,018은 **v0.4 솔버** 값으로 별도 표기. 근거: `logs/2026-08-21_expQ_nostore.txt:95-96,127,183-185`(coissue-zip 1,013 / slothy 996 / slothy2 1,018 — 5개 로그 동일). 각주: 08-17 원 로그 미보관, 당시 지퍼가 1,018이었을 가능성은 배제 불가하나 현재 아티팩트로 −2.2%는 재현 안 됨.

**A-2. yield 라벨** — `4단계_X-Wing_적용_리포트.md:14` "(fiat yield2)" → **(fiat yield3)**. b2 유닛의 실제 짝은 yield2(324.6)이므로 비율 산출 곱 개수 주석(2,193→2,203, 8:1/4:1 불변) 추가. 근거: fiat-yield3=326.4~326.5, fiat-yield2=323.5~324.7 (expQ:101,104 등).

**A-3. 정정③ "확인 요망" 마감** — `논문_데이터_표.md:23`.
로그 5회 모두 두 루틴이 병행 출력됨: 루프 평균 루틴 x1000→5,504 / x10→357,507~511 vs N=100 중앙값−25 → **5,526(5회 정확 일치)** / 357,475~357,500. 즉 5,504/357,510은 "다른 회차"가 아니라 **같은 run의 다른 측정 루틴**. 이 근거로 ⚠블록을 "확정(2026-08-24)"으로 바꾸고 로그 경로 병기. 357,474 원 로그(08-16)는 미보관, 재측정 편차 +1~+26 cyc(≤0.007%)임을 명시.

덤(같은 김에): `논문_데이터_표.md:316`·게이트 리포트 `:329` "최강 순차 626(397+229)" → **627(397+230.25)**, 1.09배 불변. F2/T4-c의 J 은닉 27% → 41차 J-solo 현장 실측 기준 **28%**(A=310)로 통일하고 27%의 분모(단독 yield2 324.6)를 각주로.

## 6. 파트 B — 재측정

### B-1. 실험 AF — 파일럿 슬롯 정본 재확정 (하네스 변경 없음, ~1시간)

- **질문**: §7 실험 4~7 슬롯의 현재 정본 중앙값은 무엇이며, 현 생성기 산출물은 e2의 커널과 바이트 동일한가.
- **사전등록에 미리 고정할 판정**: 08-21~24 로그 5회와 새 2회가 ±2 cyc/op 안에서 일치하면 **재측정값을 §7 정본으로 채택**, 08-16 값은 "재생성 전 커널 측정치"로 강등(삭제 금지).
- **절차**:
  1. 현 펌웨어 그대로 독립 플래시/실행 2회 → `logs/2026-08-24_expAF_pilot_rebaseline_run{1,2}.txt`. 대상 슬롯: mul256-flat/flat6, theta-frag, round-frag, round6-frag, stitched-*, stitched-k2, fiat-base/yield2/yield3, coissue-zip/slothy/slothy2, bfull-seq/slothy, A-tax(mul256 full/budget).
  2. **생성기 결정성 검사**(41차 expj_board.s 선례): `stitch_zip.py` 재실행 산출물 vs `e2_studio/.../src/gen/stitched.s`·`mul256_flat.s` diff. 동일→"현 생성기=정본" 기록. 상이→e2 사본을 동결 아티팩트로 보존하고 차이를 기록(어느 쪽도 조용히 덮어쓰지 말 것).
- **반영**: 리포트 §7에 v1.6 정정 블록 — 실험 4~7 표를 재측정값으로 갱신(±1 run 재현 병기), 실험 5 슬롯 회계 문단을 재유도(절감 256 > A 유휴 190 → "A 유휴 소진율" 서사 폐기, 은닉률 = 절감/min(A,B) 단일 잣대 + B 유휴가 A를 흡수한다는 관찰로 교체), 실험 3 A-세금은 "−2.2%~+1.5% 요동, ≈0"으로. `논문_데이터_표.md` T2 소규모 행·F3 계보(799→781, 681→658~662)에 "커널 세대" 각주.
- **동시에 얻는 것(T4-b)**: 같은 두 run의 base/yield2/yield3 쌍으로 T4-b를 **한 빌드 쌍 + 빌드 간 범위(+12.1~12.9%)** 표기로 교체. 결론(구조적, 레지스터 수 무관) 불변 확인.

### B-2. 실험 AG — 현재 재료로 솔버 vs 지퍼 재대결 (솔버 수 시간)

- **질문**: B(round6)가 19 cyc 빨라진 현재 재료에서도 SLOTHY가 지퍼 이상인가. (T2의 −3.5%는 08-16 세대 한정 — 현재 지퍼 658~662 vs 08-16 B 내장 솔버 656.)
- **사전등록 판정**: 같은 펌웨어에서 `솔버 ≤ 지퍼`면 3단계 성공 조건 유지(수치 갱신), `솔버 > 지퍼`면 **부정 결과로 정직 기록**(08-16 동세대 비교는 각주로 존치).
- **절차**:
  1. `slothy-2stream/gen_exp_b.py`로 exp_b.s를 **현재 round6(647명령)** 재료로 재생성. A는 기존 flatY 유지(단일 변수 = B 세대).
  2. WSL에서 `run_exp_b.py` 솔브(수 시간, 로그 `exp_b_20260824.log`로 별도 보존 — 기존 exp_b.log 덮어쓰기 금지).
  3. `gen_board_bfull.py` → 보드 실측 2회: bfull_seq / bfull_slothy / (같은 펌웨어의) stitched-round6.
- **반영**: T2 B-full 행을 같은 펌웨어 3값으로 교체(순차·지퍼·솔버 + 세대 명시), 로드맵 변경 이력 새 차수.

### B-3. 실험 AI — AE2 encaps 불균형 분해 (하네스 수정 필요)

- **질문**: F→U encaps 손실 442k는 keygen 55k의 8.1×인데 job 수는 2×뿐이다. 초과 ~330k는 어디서 오나. (AE 파일럿도 730k vs 2×206k로 동일 패턴 — dispatcher 보정으로 안 사라졌음.)
- **사전등록 분기(빈 구간 없이, 41차 교훈)**: ⓐ 2-job 융합 경로 자체(교대 스케줄/상태 2벌) ⓑ 잔여 ladder 직렬화 ⓒ x4·스티칭 상호작용 ⓓ 위 합으로 설명 안 되는 잔여 → 미분류로 기록.
- **절차**: `harness_expAE_staged.c` 사본으로 encaps 변형 2개를 같은 ELF에 추가 —
  1. **U₁**: job 0만 융합, job 1은 scalar direct(단일 변수: 2-job 융합 제거). 예측치 미리 기록: ⓐ가 원인이면 손실 ≈ keygen 손실 + scalar job 1 비용.
  2. **구간 프로브**: 융합 구간/잔여 ladder 구간 DWT 분리 계측(F와 U 각각) → 초과 330k를 구간에 귀속.
  - F→U, F→U₁ ABBA N=100, 독립 2회. 정확성 게이트는 AE2와 동일(RFC7748/SHA3/x4/U KAT, 8-seed full output/reject, stack/canary, `harness_fails=0`).
- **반영**: `measure-harness/2026-08-24_expAI_results.md` 신설 + T13에 원인 귀속 한 줄. **판정(U 기각)은 이미 확정 — 이 실험은 판정 번복용이 아니라 논문 §해석의 근거 확보용**임을 사전등록에 명시.

## 7. 완료 체크리스트

- [x] 파트 A 3건 + 덤 2건: 정정 블록·근거 로그 병기 완료, 백업 존재
- [x] AF: run 2회 로그 보존, 생성기 diff 결과 기록, §7 v1.6 정정 블록, T4-b 쌍 통일
- [x] AG: 솔버 로그·재생성 .s 보존, 같은 펌웨어 3값으로 T2 갱신
- [x] AI: prereg 분기 → 결과 md, 초과 330k 귀속 또는 "미분류" 명기
- [x] 로드맵 "현재 상태" + 변경 이력 새 차수(무엇을 재고 무엇이 바뀌었는지 한 줄씩)
- [x] `paper/`·발표자료 미접촉 확인 (수치 갱신은 별도 세션에서)
