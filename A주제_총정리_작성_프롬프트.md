# A주제 총정리 작성 프롬프트 — 육하원칙·원인·해결·결과

> 목적: 이 파일 하나를 새 세션에 제공해도 A주제의 배경부터 2026-08-24 최종 재측정까지
> 근거를 다시 확인하고, 정확한 한국어 총정리 문서를 만들 수 있게 한다.
>
> 이 프롬프트는 **총정리 문서 작성용**이다. 새 성능 주장을 만들거나 누락된 측정을
> 추정으로 메우는 지시가 아니다.

---

## 1. 임무

`A주제/`의 최신 정본·사전등록·실보드 로그를 읽고, A주제 연구를 처음 보는 사람도
다음 질문에 답할 수 있는 상세 총정리를 작성하라.

1. **누가(Who)** 무엇을 수행했고, 각 도구와 구현은 어떤 역할을 했는가.
2. **언제(When)** 어떤 문제를 발견하고 어떤 순서로 실험·정정했는가.
3. **어디서(Where)** 어떤 보드·코어·메모리 배치·툴체인에서 측정했는가.
4. **무엇을(What)** 최적화하려 했고 실제 연구 질문과 기여는 무엇인가.
5. **왜(Why)** 기대한 상한을 충분히 회수하지 못했으며, 각 병목의 근거는 무엇인가.
6. **어떻게(How)** 원인을 분리하고 어떤 해결책을 시도했으며 무엇을 채택·기각했는가.
7. 최종적으로 **무엇이 성공했고 무엇이 실패했으며 어떤 수치가 논문 대표 결과인가.**

출력 파일은 `A주제/A주제_총정리_육하원칙.md`로 한다. 이미 파일이 있다면 먼저
`A주제_총정리_육하원칙.md.bak-YYYYMMDD-HHMMSS`로 백업한 뒤 갱신한다.

`A주제/paper/`와 `A주제_발표자료.html`은 수정하지 않는다.

---

## 2. 절대 원칙

### 2.1 측정·추정 분리

- 실보드에서 직접 확인한 값만 `실측`, `확정`, `성공`, `실패`라고 쓴다.
- 호출 수 확장, primitive 합산, 상한 계산은 각각 `투영`, `혼합 장부`, `상한`으로
  표시한다. 이들을 `종단간 실측`이라고 부르지 않는다.
- 직접 측정하지 않은 코어·보드·에너지·내부 귀속은 반드시 **미측정**이라고 쓴다.
- 합리적으로 보이더라도 빈 값을 추정으로 채우지 않는다.
- 실패·음성 결과·사전등록 이탈을 삭제하거나 완곡하게 바꾸지 않는다.

### 2.2 수치 계보 분리

- **08-16 역사 커널 세대**와 **08-24 현재 생성 아티팩트**를 한 표의 동일 모집단처럼
  섞지 않는다.
- 08-16의 `727/681/657cyc`는 같은 역사 세대의 순차/지퍼/솔버 비교다.
- 08-24 현재 같은 빌드 AG 정본은 `727.036/662.35/654.036cyc`다.
- AF에서 보인 기존 스케줄 `656.011cyc`와 AG 새 솔브 `654.036cyc`를 구분한다.
- 코이슈 T2의 지퍼는 약 `1,013cyc`, v0.3 솔버는 `996cyc`, v0.4 솔버는
  `1,018cyc`다. **1,018을 지퍼라고 쓰지 않는다.**
- AF의 U8-only-ITCM 혼합 배치와 AI의 all-ITCM ELF는 서로 다른 빌드다.
  두 빌드의 `P8−2P4` 부호를 직접 비교해 하나의 효과처럼 합치지 않는다.
- stack high-water도 반드시 ELF 세대와 묶는다. AE2 flash ELF는 `9,012 B`, AF all-U
  ITCM 대표 ELF는 두 run 모두 `9,028 B`, AI 계측 ELF는 `9,020 B`다. 서로 대신 쓰지
  않는다. 특히 `논문_데이터_표.md` T13의 AF 표 뒤에 붙은 `9,012 B`는 AE2 출처가 섞인
  문장이므로 AF 결과를 설명할 때는 원시 로그의 `9,028 B`로 바로잡고 불일치 장부에 남긴다.
- 최초 AE 파일럿은 `2026-08-24_expAE_true_u_run2.txt`만 보존됐고 run1은 없다.
  AE→AE2 dispatcher 감소량은 독립 2회 재현값이 아니라 **단일 보존 AE run 기반 진단값**이다.
- AE2와 AF Flash는 모두 U가 flash에 있는 조건이지만 같은 ELF가 아니며 하네스·배치
  계보도 다르다. 두 빌드의 차이는 배치·정렬·빌드 민감성의 보조 관찰로만 쓰고
  단일변수 인과로 쓰지 않는다.

### 2.3 분모와 부호

- 종단간 비율 분모는 X-Wing 연산 전체 사이클 `D`다.
- 로컬 은닉률, 슬롯 낭비율, A-세금, 유닛 절감률은 각자의 분모를 명시한다.
- 절감률의 양수는 개선, `F→U` 표의 음수 절감은 악화다.
- `16.19%`, `15.82%`, `13.65%`, `8.89%`는 실제 종단간 대표 성과가 아니다.
- 현재 논문 대표 종단간 성과는 X/Y/C8의 **2.85/1.54/3.25%**다.

### 2.4 재현성과 인용

- 숫자가 있는 모든 표에는 바로 아래에 `근거` 행이나 문단을 두어 파일 경로를 쓴다.
- 중요한 최종값은 가능하면 결과 문서와 원시 로그 양쪽에서 확인한다.
- 소수점 재계산 시 원시 cycle을 먼저 쓰고 마지막에만 반올림한다.
- 두 독립 run, KAT/mismatch, `harness_fails=0` 통과 여부를 함께 확인한다.
- 원시 로그가 보존되지 않은 값은 `원 로그 미보관`이라고 쓴다.
- 사람 이름은 자료에 명시된 경우만 쓴다. 없으면 `연구 수행자` 또는 `이 프로젝트`라고
  표현하며 임의로 저자명을 만들지 않는다.

---

## 3. 근거 파일 우선순위

충돌 시 아래 순서를 따른다. 단, 원시 로그가 결과 문서의 전사 오류를 명백히 반증하면
로그를 우선하고 불일치 장부에 기록한다.

사전등록은 측정 전 계획과 판정 규칙의 근거이고, 결과 문서는 실제 측정값과 수행 결과의
근거다. 결과가 사전등록 분기와 다르면 어느 하나로 덮어쓰지 말고 `계획 → 실제 수행 →
이탈 이유와 영향`을 함께 기록한다.

1. `A주제/논문_데이터_표.md` — 2026-08-24 현재 수치 정본과 분모 규칙
2. 최신 재측정 결과
   - `A주제/measure-harness/2026-08-24_expAF_pilot_rebaseline_prereg.md`
   - `A주제/measure-harness/2026-08-24_expAF_pilot_rebaseline_results.md`
   - `A주제/measure-harness/2026-08-24_expAG_current_round6_solver_prereg.md`
   - `A주제/measure-harness/2026-08-24_expAG_results.md`
   - `A주제/measure-harness/2026-08-24_expAI_encaps_attribution_prereg.md`
   - `A주제/measure-harness/2026-08-24_expAI_results.md`
   - `A주제/measure-harness/2026-08-24_expAF_repro_and_u8_prereg.md`
   - `A주제/measure-harness/2026-08-24_expAF_results.md`
3. 단계별 종합 문서
   - `A주제/1단계_게이트_리포트.md`
   - `A주제/4단계_X-Wing_적용_리포트.md`
   - `A주제/4단계_상수시간_논증.md`
   - `A주제/분모_정의_통일.md`
4. 실제 통합 결과
   - `A주제/measure-harness/2026-08-21_expAA_c8_store_fusion_prereg.md`
   - `A주제/measure-harness/2026-08-21_expAB_xwing_endtoend_prereg.md`
   - `A주제/measure-harness/2026-08-21_expAA_AB_results.md`
   - `A주제/measure-harness/2026-08-21_expZ_results.md`
   - `A주제/measure-harness/2026-08-24_expAE_true_u_endtoend_prereg.md`
   - `A주제/measure-harness/2026-08-24_expAE2_results.md`
5. `A주제/PQC_스티칭_연구_로드맵.md` — 시간 순서와 시행착오 확인용
6. `A주제/2026-08-24_기록_재현성_감사.md`와
   `A주제/재측정_작업_프롬프트.md` — 정정 계보와 완료 조건 확인용
7. 각 결과 문서가 지목한 `A주제/measure-harness/logs/*.txt` — 최종 원시 대조

게이트 리포트 §1~§4·§6은 판정 당시 동결부다. 후속 현재값을 설명할 때는 §7 및 최신
결과 문서를 사용하되, 동결부의 역사적 판정을 현재값으로 몰래 다시 쓰지 않는다.

---

## 4. 작성 전 수치 감사 절차

본문부터 쓰지 말고 먼저 아래 절차를 수행하라.

### 4.1 파일·세대 감사

1. 위 근거 파일이 모두 존재하는지 확인한다.
2. 각 문서의 날짜, 실험 이름, ELF 세대를 표로 적는다.
3. `08-16 역사`, `08-21 통합`, `08-24 AF`, `08-24 AG`, `08-24 AI`로 계보를 분리한다.
4. 같은 이름의 커널이라도 ELF·입력·배치가 다르면 별도 행으로 둔다.
5. 원시 로그가 없는 값과 생성 원본이 없는 값을 미확인 장부에 올린다.

### 4.2 원시 로그 게이트 감사

명령은 `A주제/`가 바로 보이는 프로젝트 루트에서 실행한다. 먼저 현재 위치와 대표 파일
존재 여부를 확인한 뒤 다음 문자열을 `rg -n`으로 감사한다.

```powershell
Get-Location
Test-Path -LiteralPath A주제/논문_데이터_표.md

rg -n "KAT ALL PASS|mismatch|harness_fails *= *0|stack-fails|stack-highwater|HARNESS DONE|bfull_slothy diag" `
  A주제/measure-harness/logs/2026-08-21_expAA_c8_run1.txt `
  A주제/measure-harness/logs/2026-08-21_expAA_c8_run2.txt `
  A주제/measure-harness/logs/2026-08-21_expAB_xwing_run1.txt `
  A주제/measure-harness/logs/2026-08-21_expAB_xwing_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAE_true_u_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAE2_true_u_direct_finish_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAE2_true_u_direct_finish_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_flash_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_flash_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_itcm_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_itcm_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_u8only_itcm_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_u8only_itcm_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_pilot_rebaseline_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAF_pilot_rebaseline_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAG_current_round6_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAG_current_round6_run2.txt `
  A주제/measure-harness/logs/2026-08-24_expAI_encaps_attribution_run1.txt `
  A주제/measure-harness/logs/2026-08-24_expAI_encaps_attribution_run2.txt
```

유효 run은 KAT와 해당 실험 mismatch가 통과하고 `harness_fails=0`인 run뿐이다.
CPU 저속 경고 같은 수집기 stderr가 있어도 `HARNESS DONE`과 전체 gate가 있으면 경고와
유효성 판단을 함께 기록한다. 중간 fault나 실패 run은 성능 표에서 제외한다.

### 4.3 산술 재계산

아래 값은 결과 문서의 표를 그대로 복사하지 말고 한 번 다시 계산한다.

- 슬롯 낭비율: `1 − insts/(2×cycles)`
- 은닉률: `(A+B−stitched)/min(A,B)`
- 솔버 대 지퍼: `solver/zip−1`
- 종단간 절감률: `saved/D`
- A-세금: `yield/base−1`
- 두 구간 합 검산: `total = fused + residual + other`

반올림 전 값과 반올림 후 표시값을 구분한다. 재계산이 문서와 다르면 임의로 맞추지 말고
`불일치 감사` 절에 양쪽 값·공식·선택 근거를 쓴다.

### 4.4 작성 전 불일치 장부

최소 다음 정정을 확인한다.

- T1의 `5,504/357,510`과 `5,526/357,474`는 측정 루틴·정본 계보가 다르다.
  현재 표의 정본은 `5,526/357,474`다.
- 보관 자료에서 T1 두 루틴을 직접 대조할 수 있는 raw 로그는 4개다.
  expT 파일에는 T1 raw 행이 없어 “5개 원시 로그 모두 확인”이라고 쓰지 않는다.
- `326.5cyc`는 yield3이고, b2와 실제 짝인 yield2 정본은 약 `324.6cyc`다.
- encaps 곱 환산은 `2,193→2,203`이지만 8:1/4:1 선택은 바뀌지 않는다.
- 실험 J 직접 슬롯은 A=309, B=752, 절감=86이므로 은닉률은 `27.8%`다.
  J-solo의 A=310·28.1%는 다른 래퍼 경계의 역사 대조다.
- 08-16 최강 순차는 `397+230.25≈627cyc`이며 681/627≈1.09다.
- 과거 “A 유휴 슬롯 87% 소진” 서사는 현재 커널에 적용할 수 없어 철회됐다.
- `논문_데이터_표.md` T13의 `stack high-water 9,012 B`는 AE2 ELF 값이다. 바로 앞 AF
  all-U ITCM 표의 stack 값은 두 원시 run 모두 `9,028 B`이므로 출처 혼합으로 기록한다.
- 최초 AE 파일럿의 run1 로그는 미보관이고 run2만 존재한다. 최초 AE 손실과 AE2를
  비교한 dispatcher 감소량을 두-run 재현값 또는 최종 대표값으로 승격하지 않는다.
- AF 재기준선은 ±2cyc 이탈 시 정본을 갱신하지 않고 ELF·배치·정렬·클록을 먼저
  분해한다는 사전등록 분기를 그대로 수행하지 못했다. 과거 ELF·map 부재 때문에 원인을
  미측정으로 남기고 세대를 분리한 뒤 현재 ELF 값을 채택한 절차적 이탈을 명시한다.

---

## 5. 반드시 재확인할 숫자 앵커

아래는 현재 문서에서 기대되는 값이다. 최종 보고서에 넣기 전 지정 근거에서 다시
확인하라. 다르면 조용히 고치지 말고 불일치 장부를 먼저 작성한다.

### 5.1 환경·게이트

| 항목 | 확인할 값 |
|---|---|
| 보드 | EK-RA8M1, Cortex-M85, CPUCLK 480MHz |
| 툴체인 | FSP 6.5.0, GCC 13.2.1 `-O2` |
| 배치 | 실험별 ELF/map 기준. 대표 AF/AI의 U hot asm은 ITCM, AE2·AF Flash 진단의 U는 code flash; 데이터·측정 스택은 DTCM |
| 측정 | DWT CYCCNT, N=100 중앙값, calib 25 차감 |
| X25519 K1 | 357,474cyc, 366,934 insts, IPC 1.03, 슬롯 낭비 48.7% |
| Keccak K2 | 5,526cyc, 9,162 insts, IPC 1.66, 슬롯 낭비 17.1% |
| X-Wing 전체 분모 D | keygen 794,198 / encaps 1,175,479 / decaps 868,772cyc |
| 이득 상한 | 26.4 / 21.2 / 24.2%, 단순 평균 23.9% |
| 1차 판정 | 15~30% 구간이므로 2주 파일럿 |

근거: `1단계_게이트_리포트.md` §1~§4, `논문_데이터_표.md` T1.

### 5.2 스티칭·솔버

| 비교 | 순차 | 지퍼 | 솔버 | 해석 |
|---|---:|---:|---:|---|
| B-mid 156명령 | 110.06 | 88.07 | 85.06 | 솔버가 지퍼보다 약 3.4% 빠름 |
| B-full 08-16 역사 | 727.0 | 681 | 657.0 | 같은 역사 세대, 현재 정본과 혼용 금지 |
| B-full 08-24 AG | 727.036 | 662.35 | **654.036** | 새 솔버가 지퍼보다 **1.255%** 빠름 |
| 코이슈 v0.3 | 약 1,365 | 약 1,013 | 약 **996** | 지퍼 대비 −1.7%, 은닉 58→60% |
| 코이슈 v0.4 | — | 약 1,013 | 약 **1,018** | 모델 보정판이지만 실리콘에서는 지퍼보다 느림 |

AG는 A 540 + B 644 = 1,184명령이다. 프롬프트에 남은 B 647은 함수 래퍼 3명령
포함 수다. 재생성 `exp_b.s`는 기존 입력과 SHA-256이 같았지만 새 솔브가 다른 더 좋은
스케줄을 찾았다. 모델 599cyc 대 실보드 654.036cyc의 차이도 설명한다.

근거: `논문_데이터_표.md` T2, `2026-08-24_expAG_results.md`.

### 5.3 회수가 얇아진 원인

| 원인 | 실측·관찰 | 현재 결론 |
|---|---|---|
| 스필 자기잠식 | 실험 E 1,573→1,521cyc, 3.3% | 플랜 A 실전 재료는 회수 폭이 작음 |
| 레지스터 양보 세금 | 288.68→323.68(+12.124%, yield2), 325.68(+12.817%, yield3) | 실곱의 구조적 비용 |
| 실곱 LSU 경합 | A=309, B=752, 절감86, 은닉27.8% | 순수 MAC 58%보다 크게 낮음 |
| MVE `vstrw` 차단 | 합성에서는 은닉 −1%; 혼합 Q 직접 기여 110cyc | 121×2=242 전량 귀속 모델은 반증 |
| 미규명 노출 | Q 잔여 113cyc | 확정 원인으로 포장 금지 |
| 스칼라×스칼라 곱셈기 쟁탈 | 611+600=1,211 대 교차1,217, 은닉 −1% | NTT를 스칼라 짝으로 쓰면 부적합 |
| 스칼라×MVE 파이프 분리 | 611+901=1,512 대 교차1,135, 은닉62% | 연산명이 아니라 실행 파이프 공유가 핵심 |
| 코드 배치 | U8 flash의 `P8−2P4` 114k대가 ITCM에서 약 6k대로 감소 | 큰 U8 working set×flash 배치가 과거 큰 손실 원인 |
| flash 빌드 민감성 | AE2 keygen 손실 54,814/54,784 대 AF Flash 41,566/41,624cyc | 약 13.2k 차이는 보조 관찰; 서로 다른 ELF이므로 원인 단일 귀속 금지 |

원인 설명은 `증상 → 단일변수 실험 → 반증/확정 → 해결 선택` 순으로 쓴다.

### 5.4 플랜 B·NTT·실제 통합

| 항목 | 기준 | 개선 | 성격 |
|---|---:|---:|---|
| 4-way MVE Keccak | 230.3cyc/round·state, 5,526cyc/perm | 188.5, 4,524(−18%) | 독립 실측 성과 |
| forward NTT primitive | 5,354.012 | 2,814.027(−47.44%) | primitive 실측 |
| inverse NTT primitive | 6,586.013 | 3,294.030(−49.98%) | primitive 실측 |
| explicit scalar rev4 | — | 608.032 | glue 비용 |
| X/Y full ML-KEM NTT 합계 절감 | — | 18,772 / 16,177 / 24,204 | keygen/encaps/decaps, 종단간 전체 아님 |
| X/Y/C8 실제 X-Wing 절감 cycle | — | 22,637~22,651 / 18,046~18,052 / 28,280 | 실제 통합 |
| **대표 종단간 절감률** | — | **2.85 / 1.54 / 3.25%** | 최종 채택값 |

C8은 pqmx의 current-order structure-store를 재사용해 scalar `rev4`를 없앤 해결책이다.
`16.82/15.61/16.13%(평균 16.19%)` 혼합 장부는 서로 다른 통합 수준을 합한 값이므로
실제 종단간 성과로 쓰지 않는다.

### 5.5 AF 현재 파일럿 재기준선

| 항목 | 현재 두 run 정본(cyc) |
|---|---:|
| mul256 flat | 377.66 |
| theta 단독 / stitch | 70.21 / 395.67 |
| round v4 단독 / stitch | 659.21 / 780.55 |
| mul6 / round6 / stitch6 | 394.76 / 397.13 / 662.35 |
| k=2 stitch | 999.39 |
| Fiat base / yield2 / yield3 | 288.68 / 323.68 / 325.68 |

08-21 계열과 새 두 run을 모두 ±2cyc 안으로 합치는 조건은 round6·k2에서 실패했다.
사전등록은 이 경우 정본을 갱신하지 않고 ELF·배치·정렬·클록 차이를 먼저 분해하도록
정했다. 그러나 과거 ELF·map이 보존되지 않아 분해하지 못했고, 실제 결과에서는 원인을
**미측정**으로 선언한 뒤 현재 ELF 두 run을 현재 정본으로 쓰고 과거 계열을 별도 역사값으로
분리했다. 이를 `투명하게 공개된 사전등록 판정 절차 이탈`로 명시하며, 원인 분해를
완료했다고 쓰지 않는다.
`stitch_zip.py`의 stitched 출력은 바이트 재현됐지만 flat 곱의 최초 생성 원본은 없어
그 이전 완전 계보는 **미확인**이다.

### 5.6 실제 U의 실패와 AI 귀속

| 연산 | AF run 1 | AF run 2 | 판정 |
|---|---:|---:|---|
| keygen | −26,291cyc(−1.98%) | −26,362(−1.98%) | 성능 실패 |
| encaps | −83,462cyc(−3.72%) | −83,434(−3.72%) | 성능 실패 |
| decaps | −26,302cyc(−1.88%) | −26,298(−1.88%) | 성능 실패 |

정확성 게이트는 통과했으므로 `정확성 성공·성능 실패`라고 쓴다. 과거
`−4.14/−19.78~−19.81/−3.99%`는 U assembly의 flash 배치 오류가 섞인 값이므로
대표값에서 제외한다.

- AF all-U ITCM stack high-water는 두 run 모두 `9,028 B`다. AE2의 `9,012 B`나 AI의
  `9,020 B`를 이 표의 stack 값으로 붙이지 않는다.
- 최초 AE→AE2에서 제거된 dispatcher 손실의 진단값은 keygen `151,278cyc`, encaps
  `287,778cyc`, decaps `151,394cyc`다. 최초 AE run1이 없으므로 **보존된 AE run2와
  AE2 run1을 비교한 단일-run 역사 진단**이라고 라벨한다.
- 같은 flash 분류에서도 AE2와 AF Flash의 keygen 손실 차이는 run별 `13,248/13,160cyc`,
  평균 `13,204cyc`다. 이는 배치·정렬·빌드 민감성을 뒷받침하지만 서로 다른 ELF·하네스·
  빌드 계보이고 명령열 동일성도 입증되지 않았으므로 flash 하나의 단일변수 증거라고
  쓰지 않는다.

AI의 별도 all-ITCM ELF에서 encaps는 다음과 같다.

- F→U total: `+81,722~+81,822cyc` 악화
- F→U1 total: `+26,156~+26,192cyc` 악화
- 두 번째 job을 U8로 옮긴 순효과: 약 `+55.6kcyc`
- 해당 차이의 융합 구간: `+227,678~+227,707cyc`
- 잔여 ladder 구간: 약 `−172.1kcyc`
- 합성 경계 `other`: `−27~+34cyc`, 미분류
- full encaps 안에서 x4 내부 계산량을 넘는 부분의 세부 귀속: **미측정**

위 AI 값은 AI 계측 ELF에서 `candidate−F`로 계산한 AB/BA 네 값의 범위다. AF 대표표의
`음수 절감` 표기 및 `−83,462/−83,434cyc`와 부호·ELF·계측 경계가 다르므로 직접 합치거나
AF 대표 종단간 값을 교체하지 않는다.

AI는 U 기각 판정을 번복하는 실험이 아니라, 왜 encaps 손실이 컸는지 구간을 분해한
해석 실험이다.

---

## 6. 총정리 문서의 필수 구조

### 0. 읽는 법과 한 문단 결론

- 연구 목표, 최종 성공, 최종 실패를 8~12문장으로 먼저 요약한다.
- `연산별 상한 26.4/21.2/24.2%(단순 평균 23.9%)`와
  `대표 실제 2.85/1.54/3.25%`의 차이를 바로 설명한다.
- 20% 목표는 미달했지만 자동화·원인분해·MVE/NTT 통합 성과를 분리해 평가한다.

### 1. 육하원칙 한눈에 보기

표 한 개로 `누가/언제/어디서/무엇을/왜/어떻게/결과`를 정리한다.

- **누가:** 연구 수행자, 구현/측정 하네스, SLOTHY가 맡은 역할. AI나 도구를 연구의
  저자로 표현하지 않는다.
- **언제:** 2026-08-10 사전등록·준비부터 08-24 AF/AG/AI 마감까지.
- **어디서:** EK-RA8M1 Cortex-M85 480MHz, ITCM/DTCM, e2 studio/FSP/GCC, WSL2 SLOTHY.
- **무엇을:** X-Wing의 X25519와 ML-KEM 내부 Keccak/NTT 구간을 명령 수준에서 겹치는
  function stitching과 그 자동화.
- **왜:** 하이브리드 키교환 추가비용을 듀얼이슈 유휴 슬롯에 숨길 수 있는지 검증.
- **어떻게:** 사전등록→실측 게이트→손 지퍼→SLOTHY→플랜 B→실제 ML-KEM/X-Wing
  통합→실패 원인 단일변수 분해→재측정.
- **결과:** 대표 종단간 2.85/1.54/3.25%, U 기각, AG 솔버 성공, 20% 미달.

### 2. 문제 정의와 기대가 생긴 이유

- Function stitching의 개념과 M85 인오더 듀얼이슈에서 의미를 설명한다.
- X25519의 48.7% 슬롯 낭비와 Keccak의 17.1%를 근거로 가능성을 설명한다.
- X25519×Keccak의 자원 상보성과 X25519×스칼라 NTT의 파이프 충돌을 대비한다.
- 상한은 가능한 최대치이지 약속된 실현 성능이 아님을 분명히 한다.

### 3. 상세 과정 — 시간순 실험 계보

다음 단계를 각각 `질문 → 구현 → 정확성 게이트 → 실측 → 해석 → 다음 결정` 형식으로
작성한다.

1. 사전등록과 K1~K4 게이트
2. 합성 마이크로벤치와 손 지퍼 스티칭
3. 레지스터 압박·스필·A-세금 발견
4. SLOTHY 2-스트림 확장과 모델 보정
5. 전체 B-full 솔버, 현재 AG 재대결
6. 플랜 B 스칼라×MVE와 4-way Keccak
7. E/I/J/N/P/Q/L로 병목을 하나씩 분해한 과정
8. T/U 후보와 투영값의 한계
9. V/W/X/Y/Z/AA/AB의 NTT·C8·실제 X-Wing 통합
10. AC/AD/AE2/AF/AI에서 실제 U가 실패한 과정과 원인 귀속
11. 08-24 데이터 세대 감사와 재기준화

단순 성공 목록이 아니라, 앞 실험의 어떤 오판이 다음 실험 설계를 만들었는지 연결한다.

### 4. 원인 분석 — 증거 기반 원인 나무

최상위 질문은 `상한 평균 23.9%인데 왜 실제 회수는 한 자릿수인가?`로 한다.

최소 다음 가지를 포함한다.

1. 레지스터 파일 부족과 스필 자기잠식
2. MVE 베이스 확보를 위한 Fiat 레지스터 양보 세금
3. 스칼라 load/store와 MVE LSU 경합
4. `vstrw`의 스칼라 발행 차단과 혼합 커널에서의 실제 기여
5. 스칼라 곱×스칼라 NTT의 곱셈기 쟁탈
6. MVE로 분리했을 때 얻는 별도 파이프 이점
7. 메모리 상주 Keccak의 명령 팽창과 수동 지퍼 한계
8. SLOTHY M85 모델의 초기 issue/latency·메모리 모델 한계
9. U8 instruction working set의 flash 배치 오류
10. 배칭·dispatcher·래퍼 경계가 만든 잘못된 귀속
11. 커널 재생성·분모 혼용·라벨 오류가 만든 기록 계보 문제
12. AE2/AF/AI의 서로 다른 ELF·stack·부호 규칙을 섞으면서 생긴 출처 오귀속

각 원인마다 `최초 가설`, `판별 실험`, `실측`, `기각된 설명`, `현재 확정 범위`,
`남은 미측정`을 표로 쓴다.

### 5. 해결방법과 채택 여부

다음 해결책을 각각 왜 시도했고 어떤 조건에서 유효했는지 설명한다.

- 플래그 안전 제약과 캐리 체인 보존
- Keccak 메모리 상주·레지스터 예산 스윕
- 손 지퍼/Bresenham과 SLOTHY 자동 스케줄
- SLOTHY M85 명령 클래스·듀얼이슈·latency 패치
- 스칼라×MVE로 레지스터 파일과 실행 파이프 분리
- 4-way MVE Keccak 배칭
- b0/b1/b2와 yield1/yield2/yield3 선택
- pqmx M85 NTT 이식과 current-order C8 store fusion
- hot code ITCM 배치와 ELF/section 감사
- ABBA, 동일 함수·동일 RNG·동일 스택, 독립 flash/run 2회
- staged checkpoint로 F/U/U1 구간 분해
- 성능 실패 후보 U를 기각하고 X/Y/C8을 대표 경로로 유지한 결정

해결책을 모두 성공으로 묶지 말고 `채택`, `부분 성공`, `진단 전용`, `기각`으로 나눈다.

### 6. 결과 표

최소 다음 표를 포함한다.

1. 환경과 정확성 게이트
2. 1단계 상한
3. 역사/현재 세대를 분리한 순차·지퍼·솔버
4. 원인별 실측과 해결책
5. MVE Keccak·NTT primitive와 실제 통합의 계층 차이
6. 최종 X/Y/C8 종단간 결과
7. U 직접 통합 실패와 AI 구간 귀속
8. 성공/실패/미측정 최종 판정표

### 7. 정확성·상수시간·재현성

- RFC 7748, SHA3, ML-KEM roundtrip/reject, 8-seed full output/reject 등 실제로 적용된
  게이트를 실험별로 구분한다.
- mismatch 0, stack/canary, `harness_fails=0`을 기재한다.
- 스티칭은 정적 명령 재배열·레지스터 재할당·고정 오프셋 스필이라는 구조 논증을 쓴다.
- 대표 expJ 두 입력에서 median Δ=0cyc였다는 실증과 그 한계를 함께 쓴다.
- 다른 보드·에너지·전력 부채널은 검증하지 않았음을 명시한다.

### 8. 최종 평가

다음 세 문장을 서로 분리해 결론낸다.

1. **성능 목표:** 20% 목표는 미달성했다.
2. **채택 성과:** 실제 X-Wing X/Y/C8은 2.85/1.54/3.25% 개선됐다.
3. **연구 기여:** 2-스트림 자동화, 인오더 듀얼이슈에서 회수가 얇은 원인의 실증적
   분해, 4-way MVE Keccak·M85 NTT/C8 통합, 사전등록·음성 결과 공개다.

`실패가 곧 무성과` 또는 `일부 primitive 개선이 곧 20% 달성`이라는 두 극단을 모두
피한다.

### 9. 남은 일과 미측정 위험

- 다른 M85 SoC 및 Cortex-M55/M52 성능
- 에너지와 PPK2 측정
- 전력 부채널
- K4 분모 정직성 추가 확인 범위
- SLOTHY 상류 패치 정리와 TRM 페어링 모델
- Q의 잔여 113cyc 및 AI x4 내부 세분
- flat 곱 최초 생성 원본 부재
- 단일 보드 일반화 한계

각 항목에 `왜 현재 결론을 뒤집는지/뒤집지 않는지`를 한 줄로 평가한다.

---

## 7. 문체와 설명 수준

- 한국어로 작성하고, 처음 등장하는 전문 용어는 한 문장으로 풀어 쓴다.
- 숫자 나열보다 인과 관계를 우선하되, 판정을 뒷받침하는 원시 cycle은 생략하지 않는다.
- 각 단계에서 `왜 다음 실험이 필요했는가`를 반드시 설명한다.
- 역사적 오류는 취소선 대신 `당시 해석 → 반증 실험 → 현재 해석` 형식으로 쓴다.
- 표 다음에는 반드시 독자가 알아야 할 의미를 2~5문장으로 해설한다.
- `최신`, `최선`, `정본`에는 날짜·ELF 세대·근거를 붙인다.
- 보고서 끝에 `핵심 숫자 15개`와 `잘못 인용하기 쉬운 숫자 10개` 체크리스트를 둔다.

---

## 8. 금지 문장·금지 혼용

다음 주장은 쓰지 않는다.

- “현재 B-full 지퍼는 681cyc다.” — 681은 08-16 역사값이다.
- “현재 솔버는 656.011cyc로 지퍼와 동률이다.” — AG 새 정본은 654.036cyc다.
- “v0.4 지퍼가 1,018cyc다.” — 1,018은 v0.4 솔버다.
- “U가 종단간 8.89% 개선했다.” — 투영이며 실제 U는 악화했다.
- “전체 스택이 16.19% 빨라졌다.” — 혼합 장부이며 완전 통합 실측이 아니다.
- “실험 5가 현재 A 유휴 슬롯의 87%를 소진했다.” — 현재 세대에서 철회된 서사다.
- “121개 `vstrw`가 각각 정확히 2cyc를 막는다.” — 혼합 커널에서 반증됐다.
- “996cyc가 853cyc 구조 하한으로 0오차 분해됐다.” — Q 이후 철회됐다.
- “U8 구조 자체가 330k 손실을 만들었다.” — flash 배치와 구간 이동 효과를 분리해야 한다.
- “AF ITCM의 stack high-water는 9,012 B다.” — 9,012 B는 AE2이고 AF ITCM은 9,028 B다.
- “AE dispatcher 감소량은 두 독립 run에서 재현됐다.” — 최초 AE는 run2만 보존됐다.
- “AF 재기준선은 사전등록 판정 절차를 그대로 충족했다.” — ±2cyc 이탈 뒤 원인 분해 대신
  원인 미측정 선언과 세대 분리를 적용한 절차적 이탈이 있다.
- “AE2와 AF Flash의 약 13.2k 차이가 flash 배치 효과를 단독으로 증명한다.” — 서로 다른
  ELF·하네스·빌드 계보의 보조 관찰이며 명령열 동일성도 입증되지 않았다.
- “M55/M52에서도 같은 비율이다.” — 미측정이다.

---

## 9. 완료 전 자체 검수

아래를 모두 만족할 때만 완료로 보고한다.

- [ ] 육하원칙 6개와 결과가 첫 표에서 모두 답변됐다.
- [ ] 2026-08-10~08-24 과정이 인과 순서로 정리됐다.
- [ ] 역사 커널과 현재 커널이 분리됐다.
- [ ] 직접 실측·투영·혼합 장부·상한·미측정 라벨이 구분됐다.
- [ ] 대표 종단간 결과가 2.85/1.54/3.25%로 명시됐다.
- [ ] U의 정확성 성공·성능 실패가 명시됐다.
- [ ] AG 727.036/662.35/654.036과 AI 구간 귀속이 반영됐다.
- [ ] AE2 9,012 B, AF ITCM 9,028 B, AI 9,020 B를 ELF별로 분리했다.
- [ ] 최초 AE run1 미보관과 dispatcher 진단값의 단일-run 한계를 명시했다.
- [ ] AF 재기준선의 ±2cyc 사전등록 판정 절차 이탈을 명시했다.
- [ ] AE2 대 AF Flash의 약 13.2k 차이를 보조 관찰로만 분류했다.
- [ ] 각 주요 수치에 근거 파일이 붙었다.
- [ ] 모든 퍼센트를 원시 cycle에서 재계산했다.
- [ ] KAT/mismatch/`harness_fails=0`이 유효 run마다 확인됐다.
- [ ] Q 잔여 113cyc, AI 내부 세분, 다른 코어·에너지가 미측정으로 남았다.
- [ ] `paper/`와 발표자료를 수정하지 않았다.
- [ ] 최종 문서에 충돌·오탈자·깨진 UTF-8이 없다.

완료 보고에는 생성한 총정리 파일 경로, 확인한 원시 로그 수, 발견한 불일치 수,
끝까지 미측정으로 남긴 항목 수를 함께 적는다.
