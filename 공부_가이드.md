# A주제 공부 가이드 — 기초 개념부터 우리 실험까지 (2026-08-20)

> 대상: 이 연구(M85 스티칭)를 **밑바닥 개념부터** 이해하려는 나(박도윤).
> 사용법: 파트 1→5 순서로 읽되, 각 절 끝의 **[우리 프로젝트에선]**으로 실험과 연결.
> 마지막 자가 점검 질문에 막힘없이 답하면 논문 집필·심사 방어 준비가 된 것.
> 심화는 `papers/` 폴더 + 각 리포트 파일로.

---

# 파트 1 — 암호학 기초

## 1.1 키 교환이 하는 일

두 사람이 도청되는 채널에서 **공유 비밀**을 만드는 것. 공개키 방식 두 계열:
- **DH(디피-헬만) 계열**: 각자 (비밀키, 공개키)를 만들고 상대 공개키 × 내 비밀키 = 같은 값.
- **KEM(키 캡슐화)**: 상대 공개키로 비밀을 "캡슐"에 넣어 보내면 상대만 열 수 있음.
  (encaps = 캡슐 만들기, decaps = 열기, keygen = 키 생성 — 우리가 재는 3연산)

## 1.2 X25519 — 고전측 스트림 A

- **Curve25519**라는 타원곡선 위의 DH. 보안 근거: 타원곡선 이산로그 문제(ECDLP).
- 핵심 연산 = **스칼라곱** `k·P`: 점 P를 비밀 스칼라 k(255비트)만큼 "더하기".
- 구현은 **몽고메리 래더**: 비트마다 똑같은 스텝(조건 스왑 cswap + 점 연산)을 255회 —
  비트가 0이든 1이든 같은 일을 하므로 상수시간(1.6절)의 뼈대.
- 래더 스텝 1회 ≈ 필드 곱셈 5회 + 제곱 4회 + 소곱 1회 + 덧뺄셈 — **곱셈 기계**다.
- **필드 산술**: 모든 수는 소수 p = 2²⁵⁵−19로 나눈 나머지. 255비트 수를 32비트
  레지스터에 담으려면 쪼개야 함 = **림(limb)** 표현:
  - fiat-crypto: 10림브 × 25.5비트 (캐리 여유를 둔 "포화 안 된" 표현)
  - Lenngren asm: 8워드 × 32비트 (꽉 찬 표현 + umaal로 캐리 전파)
- 필드 곱 1회 = 32비트 곱셈-누산(umull/umaal) 수십 개의 폭포. **[우리 프로젝트에선]**
  fiat 곱이 실험 C~M의 스트림 A. Lenngren 전체 스칼라곱 357,474cyc가 게이트 기준.
  래더 DFG 그림이 논문 Figure 1.

## 1.3 Keccak — 두 스트림을 잇는 다리

- SHA-3의 심장. **스펀지 구조**: 상태(1600비트)에 입력을 흡수(absorb)하고
  순열(permutation)을 돌리고 출력을 짜냄(squeeze).
- **Keccak-f1600 순열** = 5×5 격자 × 64비트 레인 25개, **24라운드**.
- 라운드 = θ(열 패리티) → ρ(레인별 회전) → π(자리 섞기) → χ(비선형 and/xor) → ι(상수).
- **곱셈이 하나도 없다** — 전부 XOR/AND/NOT/회전. ← X25519(곱셈 기계)와 실행 유닛이
  안 겹치는 이유 = 스티칭 짝으로 뽑은 이유. (실험 L이 이걸 정면 검증: 파트 3.5)
- **비트 인터리빙**: 32비트 머신에서 64비트 회전은 2명령이지만, 레인을
  (짝수비트, 홀수비트) 두 워드로 쪼개 저장하면 64비트 회전 = 32비트 회전 1개로 접힘.
  pqm4가 쓰는 표준 기법. **[우리 프로젝트에선]** 실험 D에서 644→570명령 절감,
  4-way MVE판도 이 표현 유지(그래서 글루 비용이 레인 전치뿐 — 실험 H).

## 1.4 ML-KEM (Kyber) — PQC측

- 보안 근거: **격자 문제(MLWE)** — 양자컴퓨터로도 어렵다고 믿어지는 문제.
- 구조: 다항식 행렬·벡터 연산. 다항식 곱셈을 빠르게 하는 도구가 **NTT**(수론적 변환,
  FFT의 정수판) — 버터플라이 연산(곱+덧뺄셈)의 나무. **NTT도 곱셈 기계**다.
- 그런데 ML-KEM 사이클의 큰 몫은 정작 **Keccak**: 행렬 A를 시드에서 펼치는
  XOF(SHAKE128, gen_matrix), 노이즈 샘플링, 해시 — 우리 실측으로 순열
  keygen 43 / encaps 44 / decaps 44회. **[우리 프로젝트에선]** 이 순열들이 스트림 B의
  일감이고, 4개씩 묶는 4-way 배칭이 gen_matrix의 병렬 XOF 구조와 자연 정합(설계서 §2).

## 1.5 하이브리드와 X-Wing

- 전환기 원칙: **PQC가 깨져도 고전이 지키고, 고전이 깨져도(양자) PQC가 지킨다** —
  둘을 함께 돌리고 결과를 **결합기**(SHA3-256)로 합침.
- **X-Wing** = X25519 + ML-KEM-768 조합의 표준화 후보. 문제: 임베디드에서 두 알고리즘
  비용이 **더해진다** → 이 덧셈을 max()로 바꾸자는 게 본 연구의 출발점.
- **[우리 프로젝트에선]** 결합기 SHA3-256도 순열 1회라 배칭 큐에 그냥 합승
  (X-Wing 리포트 §결합기).

## 1.6 상수시간 (constant-time)

- 실행 시간이 비밀에 의존하면 시간만 재도 비밀이 샌다(타이밍 부채널).
- 금지 2칙: **비밀 의존 분기** 금지, **비밀 의존 메모리 주소** 금지.
- 래더의 cswap이 분기 대신 마스크 연산인 이유, Keccak이 원래 데이터 무관인 이유.
- **[우리 프로젝트에선]** 스티칭 = 정적(컴파일 타임) 재배열이라 분기/주소를 추가하지
  않음 → 누설 추가 0. 실증: 입력 2벌 median Δ=0 (`4단계_상수시간_논증.md`).

---

# 파트 2 — 컴퓨터 구조 기초 (Cortex-M85)

## 2.1 레지스터와 어셈블리

- ARM 32비트(Thumb-2): 범용 레지스터 **r0~r12 + sp(r13), lr(r14), pc(r15)** —
  자유롭게 쓸 수 있는 건 실질 **14개**(r0-r12, lr). 별칭 주의: ip=r12, fp=r11, sl=r10
  (실험 J에서 `ip` 별칭 때문에 r12 grep이 헛짚은 함정).
- 값이 레지스터에 못 들어가면 스택에 내려놓고(스토어) 다시 올림(로드) = **스필**.
  스필은 공짜가 아니다 — 이 연구 후반부 전체가 스필 비용과의 싸움(실험 E, I).
- 곱셈-누산 명령: `umull`(32×32→64), `umaal`(곱하고 두 값 더함 — 캐리 전파에 최적).
  플래그(APSR): `adds/adcs`의 s = 캐리 플래그를 쓰고 읽음 → **캐리 체인 사이에
  플래그 건드리는 명령을 끼우면 안 됨**(스티칭 제약 — 2단계 선행 검사에서 확인).

## 2.2 파이프라인, 인오더, 듀얼이슈

- 파이프라인: 명령을 공장 라인처럼 겹쳐 실행. **인오더** = 프로그램 순서대로만 발행
  (M85). **아웃오브오더(OoO)** = 하드웨어가 알아서 재배열(인텔 x86 — 백서의 세계).
- **듀얼이슈**: 한 사이클에 명령 **2개까지** 발행 가능. 단 조건이 맞아야(페어링 규칙).
- **IPC**(사이클당 명령 수): 최대 2.0. **슬롯 낭비** = (2 − IPC)/2.
  - X25519: IPC 1.03 → **낭비 48.7%** (곱셈 의존 사슬이 길어서 짝을 못 채움)
  - Keccak: IPC 1.66 → 낭비 17.1%
  - 이 낭비가 곧 **스티칭 이득의 상한** — 게이트 산수의 전부 (T1).
- 인오더는 하드웨어가 재배열을 안 해주므로 **컴파일 타임 스케줄 품질이 곧 성능** —
  SLOTHY(파트 4)가 필요한 근본 이유.

## 2.3 실행 유닛 — "쟁탈전"의 물리학

- 발행된 명령은 종류별 유닛으로 감: **MAC**(곱셈기) · **ALU**(덧셈/논리) ·
  **LSU**(로드/스토어) · **벡터 파이프**(MVE).
- 같은 유닛을 원하는 두 명령은 같은 사이클에 못 감 = **구조적 해저드(쟁탈전)**.
- **[우리 프로젝트에선]** 이게 원인 분해 표(T4)의 물리학:
  - 스칼라 곱 × 스칼라 NTT = 같은 MAC 쟁탈 → 은닉 **−1%** (실험 L)
  - 스칼라 곱 × MVE = 곱셈기가 딴 하드웨어 → 은닉 58~62%
  - 스필 ld/st × MVE vldrw/vstrw = LSU 접점 → 은닉 27%로 하락 (실험 J)
  - **실험 N(페어링 스펙트럼)이 결정판**: MVE 클래스별로 1:1 지퍼를 재보니
    veor 99% / vldrw 99% / 회전 99%인데 **vstrw만 −1%(스칼라 발행 완전 차단)** —
    코이슈 천장 60%의 실체가 "vstrw 121개 × 2cyc = 242cyc 노출"로 검산됨 (T5).

## 2.4 SIMD / MVE (Helium)

- **SIMD**: 한 명령으로 여러 데이터. MVE = M-프로파일 벡터 확장, **Q0~Q7** 8개의
  128비트 레지스터, 32비트 **레인 4개**씩.
- **[우리 프로젝트에선]** 레인 4개 = **독립 Keccak 상태 4개**(4-way 배칭). 같은
  자리 레인끼리 같은 연산이므로 회전량도 균일해짐 — 5-way 시도가 깨졌던 비정합의
  해법. 보너스: 데이터가 전부 Q에 있으니 **GP 레지스터를 안 먹음**(베이스 포인터
  2개만) → 레지스터 쟁탈 원천 차단 = 플랜 B의 존재 이유.
- 벡터 load/store 주소 지정: `vldrw [rN, #±508]` — 부호 있는 7비트×4 오프셋.
  이 ±508 덕에 베이스를 영역 **중앙**에 앵커하면 커버가 2배(실험 G의 5→3→2 축소).

## 2.5 메모리 계층과 TCM

- 플래시(느림, 캐시 통과) ↔ SRAM ↔ **TCM**(Tightly Coupled Memory): 코어 직결
  0-wait — ITCM(명령용 64KB)·DTCM(데이터용). 캐시가 없으니 **타이밍이 결정적** —
  측정 재현성과 상수시간 논증 둘 다 TCM 덕을 봄.
- **[우리 프로젝트에선]** 핵심 커널 ITCM·데이터 DTCM 배치가 측정 위생의 기둥.
  ITCM 64KB 예산 때문에 구실험 커널은 플래시(.text)로 옮김. (이 보드 개체는 ITCM
  8바이트 2곳 고착 결함 → itcm_pad.c로 회피 — 하드웨어는 이상적이지 않다.)

## 2.6 사이클 측정 (DWT)

- **DWT CYCCNT**: 코어에 내장된 32비트 사이클 카운터. 구간 전후 값 차 = 소요 사이클.
- 위생: N=100 반복 **중앙값**(인터럽트 스파이크 제거), 빈 구간 측정치(calib) 차감,
  루프 시작 `.balign 16`(정렬 따라 ±1cyc), **정답 검증과 측정을 같은 하네스에서**
  (빠른데 틀린 코드 방지). 전부 `측정_위생`+게이트 계획서에 사전 등록.

---

# 파트 3 — 스티칭: 이 연구의 중심 아이디어

## 3.1 한 문장 정의

**독립적인 두 계산의 명령을 손가락 깍지 끼듯 교차 배치해, 한쪽이 파이프라인을
못 채우는 사이클에 다른 쪽 명령을 밀어 넣는 것.** (인텔 백서 323686, x86 AES×SHA)

## 3.2 산수

- 이상: `T_stitch ≈ max(T_A, T_B)` → 절감 상한 = `min(T_A,T_B) / (T_A+T_B)`.
- **은닉률** = (순차합 − 교차실측) / min(A,B) — "짧은 쪽이 얼마나 공짜가 됐나".
  100%면 완전 은닉. 우리 수치는 전부 이 잣대(T4, F2 사다리).

## 3.3 왜 어려운가 (이 연구가 정량화한 세 가지 벽)

1. **레지스터 예산**: 두 계산이 14개 GP를 나눠 써야 함. 한쪽이 포화(fiat 곱)면
   상대 몫을 뺏어야 하고 → 스필 증가 → **양보 세금**(실험 I: +12~13.5%,
   레지스터 수와 무관하게 live-range가 지배 = 구조적).
2. **유닛 쟁탈**(2.3절): 짝 선택의 문제. 곱셈기끼리 만나면 0, 분리되면 60%+.
3. **플래그**: 캐리 체인(adds→adcs)을 끊는 명령 삽입 금지 — Keccak 비-S형이라 통과.

## 3.4 지퍼 vs 솔버

- **지퍼**(stitch_zip.py): A, B 명령을 기계적으로 1:1(또는 Bresenham 비율) 교차.
  레지스터를 미리 갈라놔야 함(분할 전제). 손 스티칭의 자동화판.
- **솔버**(SLOTHY): 의존성 그래프를 보고 순서+레지스터를 **전역 최적화** — 분할
  전제가 없어 지퍼가 못 푸는 배치(flatY 재료)도 풂. 실측 전 규모에서 지퍼보다
  −2~3.5% 추가 (T2).

## 3.5 우리의 최종 그림 (F2 사다리로 외우기)

```
-1%     스칼라곱 × 스칼라NTT   (같은 곱셈기 = 벽)
27%     실제 fiat곱 × MVE Keccak (스필 ld/st가 LSU에서 부딪힘)
58~60%  순수 MAC체인 × MVE Keccak (지퍼 → 솔버 v0.3)
62%     MAC체인 × MVE NTT형     (곱셈기 분리 증명)
천장의 정체 = vstrw가 스칼라 발행을 막는 2cyc × 개수 (실험 N, T5)
```
최종 제품 수치: keygen **7.7%** / encaps **5.5%** / decaps **7.2%** (T3) — 상한(keygen 26.4 /
encaps 21.2 / decaps 24.2, 평균 **23.9%**)의 **28%만 회수**했고,
그 간극의 **원인이 전부 측정으로 분해**돼 있다는 것이 논문의 심장.
분모는 전부 **X-Wing 연산 1회 전체 사이클**(794,198 / 1,175,479 / 868,772)로 통일돼
있다 — 2026-08-20 이전 문서의 11.4 / 7.3%는 ML-KEM 비-Keccak 구간을 분모에서 뺀 값이라
상한과 나눌 수 없었다. 규칙과 전량 대조표: `분모_정의_통일.md`.

---

# 파트 4 — SLOTHY와 자동화 (기여 ③)

## 4.1 슈퍼최적화가 뭔가

컴파일러 휴리스틱 대신 **제약 솔버**(구글 OR-Tools CP-SAT)로 "이 명령들의 합법적
배치 중 가장 빠른 것"을 **탐색**. SLOTHY는 순서(스케줄링)+레지스터(할당)를 함께 푼다.
큰 커널은 통째로 못 푸니 창문으로 잘라 반복(**분할 휴리스틱** — 경계 스톨이 모델
예측 갭 5~7%의 원인).

## 4.2 마이크로아키텍처 모델 — 정확도가 전부

솔버는 **모델이 말해주는 대로**의 최적을 찾는다. 모델이 틀리면 자신만만하게 느린
코드를 낸다. 실측 사례로 체득할 것:
- v0.2(듀얼이슈 무제약): 예측 611 → 실측 **1,319** (지퍼 1,018에 30% 패배)
- v0.3(umaal 누산 포워딩 수정 + 지퍼 입력): **996** — 지퍼 역전
- v0.4(vstrw 차단 반영): 예측 갭 27%→12%, 재스케줄 이득은 없음 → **996이 실리콘
  최선, 남은 노출은 스케줄이 아니라 설계(vstrw 수)의 몫**이라는 결론까지 도달
"모델 수정 → 예측 → 실측 검증"의 루프 자체가 방법론이다.

## 4.3 우리가 얹은 것 (논문에서 방어할 목록)

- 명령 클래스 19+종 추가(umaal/캐리/즉치형/시프트), 플래그를 의존성으로 모델링
- issue_rate 1→2 + 스칼라 제2유닛, MAC 누산기 포워딩(v0.3), vstrw 차단(v0.4)
- 2-스트림 입력 표현: 연접/지퍼면 충분 — 의존성 분석이 독립성을 스스로 발견
- 안전 변환 3칙(ldrd 분해+베이스 순서 / 스필 슬롯 고유화) ← 상류 이슈 3건의 우회책
  (제출 초안: `slothy-2stream/upstream_issues.md`)

---

# 파트 5 — 방법론 (심사 방어의 무기)

- **사전 등록**: 판정 기준(게이트 ≥30/15~30/<15%)과 퇴로를 **측정 전에** 문서로 서약
  → 사후 합리화 차단. 실제로 퇴로 B가 발동됐고 그대로 논문 서사가 됨.
- **정직한 베이스라인**: 비교 대상(순차)도 최강으로 최적화(인텔 p.10 원칙).
- **대조군**: 굵은 교대(함수 단위 번갈아 실행) ≈ 0% (실험 K) → 이득이 "명령어 교차"
  덕임을 분리. 이게 없으면 "그냥 캐시 효과 아니냐"에 무너짐.
- **정답 검증 동봉**: 모든 측정 커널은 C 레퍼런스와 대조(mismatch=0) 후에만 인정.
- **사전 등록 방어의 사정거리는 1차 게이트까지다 (36차 감사)**: 계획서 v0.1에는
  **2차 게이트(파일럿 재판정) 기준이 없다** — 실제 기준은 리포트 §5-2(파일럿 착수 전)에
  있으나 1차와 동급의 사전 등록은 아니다. **논문에서 "사전 등록했다"는 방어는 1차 게이트에만
  걸 것.** 계획서 정오 3건(E1 임계값 문구·E2 경계 중복·E3 퇴로 A 정의)도 본문 동결 유지 +
  정오 블록 신설 방식으로 처리했다(삭제·대체는 사후 조작으로 보인다).
- **수치 정본은 "자기정합적인 체인"으로 고른다**: X25519 357,474 · Keccak 5,526은
  첫 실측(357,510 / 5,504)이 아니라 **calib 25 차감 최종치**다. 파생값이 전부 후자에서만
  맞기 때문 — idle_A 348,014, Keccak 낭비 17.1%(5,504면 16.8%로 어긋남). 둘 다 실측이므로
  "어느 회차가 정본인가"의 문제이고, 근거는 **자기정합성**. (IPC·낭비율·상한은 전부 불변)
- **자기 사전 등록을 오인용하지 말 것**: 리포트 §4가 "§5 예상과 반대 방향"이라 썼으나
  사전 등록은 X25519가 길다고 예상했고 실측이 그대로 **확인**했다 — 없는 반전을 만들어
  쓴 사례로 삭제됨. 감사에서 정정 대상 1순위는 늘 **자기 문서 인용**이다.
- 전 수치 일람: `논문_데이터_표.md` (T1~T5, F2~F3).

---

# 파트 6 — 실험 지도 (파일·개념 연결)

| 실험 | 무엇을 | 핵심 개념 | 결과 |
|---|---|---|---|
| 게이트 | 낭비 실측 → 상한 | IPC, 슬롯 낭비 | 21~26% (T1) |
| 파일럿 1~7 | 손 스티칭 사다리 | 지퍼, 레지스터 예산, k-스윕 | **08-16 역사 세대:** θ 82% 은닉 → 875→681; 현재 아티팩트는 74%, 781→662.35 |
| A/B-mid/B-full | 솔버 스케일업 | CP-SAT, 분할 휴리스틱 | 솔버 ≥ 지퍼 (T2) |
| C/D | 실재료·비트인터리브 | fiat, 표현 변환 | 630→579 (F3 끝) |
| E | 실비율 희석 | 스필 자기잠식 | 3.3% → 플랜 B 발동 |
| MVE/코이슈 | 4-way 배칭 | SIMD 레인, 코이슈 | 188.5/라운드·상태(−18%), 58% |
| F-solver | 모델 정확도 | v0.2→v0.3 | 1,319→996 |
| G/H | 통합 관문 | ±508 앵커, 레인 전치 | 베이스 2, 글루 2.5% |
| I/v0.4 | 양보 세금 | -ffixed, live-range | +12% 구조적 |
| J | 진짜 통합 | LSU 경합 | 8.1% |
| K | 대조군 3종 | 굵은 교대 | 교대 ≈ 0% |
| L | NTT 짝 | 유닛 쟁탈 | −1% vs 62% |
| M/M4 | 3연산 실비율 | 스트림 균형 | 5.5~7.7% (T3) |
| N | 페어링 스펙트럼 | vstrw 차단 | 천장 = 스토어 (T5) |

---

# 파트 7 — 읽기 순서 (papers/ + 문서)

1. 인텔 function stitching 백서(323686) — §개념, p.9 짝 유형, p.16 DFG 방법론
2. `1단계_게이트_리포트.md` → `논문_데이터_표.md` (우리 수치 감 잡기)
3. X-Wing 논문 앞부분(구조·결합기까지만 — 정독 금지 원칙 유지)
4. SLOTHY 논문 (TCHES) — 모델·제약 부분 위주
5. Becker–Kannwischer "Hybrid scalar/vector Keccak" — 우리 플랜 B의 선행
6. `slothy-2stream/README.md`(실험 일지) → `5단계_논문_골격.md`
7. **원고 정본 `paper/ko/main.tex`**(10쪽, 인용 12건). 내용 변경은 항상 `ko`에서 시작하고
   `en/`은 뒤따른다. `paper/논문_초안.md`는 피벗 전 초안이라 **대체됨**(참고용).
   남은 구멍: M55 보조 플랫폼은 **안 사기로 확정** → §한계 문구로 정리, PPK2(에너지)만 미결.

# 파트 8 — 용어 미니 사전

래더(ladder) 몽고메리 스칼라곱 루프 · 림(limb) 큰 수의 조각 · 스펀지 absorb/squeeze
구조 · XOF 길이 자유 해시 출력 · NTT 정수판 FFT · 발행(issue) 명령을 파이프에 넣기 ·
페어링 한 사이클 2명령 조건 · 구조적 해저드 유닛 쟁탈 · 스필 레지스터→스택 대피 ·
live-range 값이 살아있는 구간 · KAT 알려진 정답 테스트 · DFG 데이터 흐름 그래프 ·
코이슈 스칼라+벡터 동시 발행 · 은닉률 (순차합−교차)/min(A,B)

# 자가 점검 (막히면 해당 절로)

1. X25519 낭비 48.7%는 어떻게 나온 숫자인가? 왜 Keccak(17.1%)보다 큰가? [2.2]
2. Keccak엔 왜 곱셈이 없고, 그게 왜 스티칭 짝 선택의 근거인가? [1.3, 2.3]
3. 은닉률 −1%(스칼라 NTT)와 62%(MVE NTT형)의 차이를 하드웨어로 설명하라. [2.3]
4. 양보 세금이 "구조적"이라는 말의 근거 실험은? 왜 레지스터 수와 무관한가? [3.3, I/v0.4]
5. 굵은 교대 대조군이 없으면 어떤 반론에 무너지는가? [파트 5]
6. 솔버가 지퍼에 진 적이 있는가? 왜? 어떻게 역전했나? [4.2]
7. 코이슈 은닉 60% 천장의 실체는 무슨 명령인가? 검산 산수는? [2.3, T5]
8. 스티칭이 상수시간을 깨지 않는 이유 세 줄 요약 + 실증 수치는? [1.6]
9. 4-way 배칭에서 "글루"는 뭐고 왜 비트인터리브는 글루가 아닌가? [1.3, 실험 H]
10. 최종 5.5~7.7%가 상한 21.2~26.4%에 못 미친 원인 다섯 개를 T4 없이 말해보라. [3.5]
10-b. 두 수의 **분모가 무엇인지** 답하고, 분모를 Keccak 구간으로 좁히면 게이트 판정이
    어느 칸으로 옮겨가는지 말하라. 왜 좁은 분모를 쓰지 않았나? [분모_정의_통일.md]
11. 심사자가 "그 판정도 사전 등록된 것이냐"고 물으면 **어디까지** 그렇다고 답할 수 있나? [파트 5]
12. T1이 357,510이 아니라 357,474인 이유는? "둘 다 실측인데 왜 하나가 정본인가"에 답하라. [파트 5]
