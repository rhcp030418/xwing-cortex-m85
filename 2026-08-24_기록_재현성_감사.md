# 기록·재현성 감사 (2026-08-24)

## 범위와 처리 원칙

- 이 문서는 2026-08-24까지 보존된 로그와 현재 생성 아티팩트의 재측정을 대조한 정정 장부다.
- 같은 이름의 커널이라도 생성 시점이 다르면 같은 세대의 결과로 간주하지 않는다.
- 08-16 원 로그가 없는 값은 **역사값**으로 보존하되, 현재 아티팩트에서 재현되지 않으면 논문 대표 실측으로 쓰지 않는다.
- 이번 감사에서는 Markdown만 정정한다. LaTeX와 PDF 반영은 최종 원고 갱신 때 별도로 한다.

## 1. 확정 기록 오류

### 1.1 T2 코이슈 비교의 기준 혼합

현재 보존 로그와 아티팩트가 재현하는 동일 기준 비교는 다음과 같다.

| 항목 | 실측 | 은닉률 | 비교 |
|---|---:|---:|---:|
| `coissue-zip` | 1,013 | 58% | 기준 |
| `coissue-slothy` (모델 v0.3) | 996 | 60% | 지퍼 대비 **−1.7%** |
| `coissue-slothy2` (모델 v0.4) | 1,018 | 57% | v0.3 대비 **+2.2%** |

기존 T2의 `지퍼 1,018 → 솔버 996 (−2.2%, 57→60%)`은 v0.4 솔버의
1,018·57%와 v0.3 솔버의 996·60%를 지퍼 비교처럼 합친 기록 오류다. 따라서 T2는
`1,013 → 996 (−1.7%, 58→60%)`으로 고친다. 08-17 당시 지퍼가 1,018이었는지는
원 로그가 없어 배제할 수 없지만, 현재 보존 아티팩트로는 재현되지 않는다.

### 1.2 4단계 비율 산출의 곱 단가

`326.5cyc`는 `fiat-yield3`의 값이다. b2 유닛이 쓰는 곱은 `fiat-yield2`
`324.6cyc`이므로 encaps의 곱 개수 환산은 약 `2,193 → 2,203`으로 고친다.
8:1과 4:1 비율 판정은 변하지 않는다.

### 1.3 T4-b yield2 세금의 빌드 혼합

| 펌웨어/로그 계열 | base | yield2 | 세금 |
|---|---:|---:|---:|
| 초기 I 계열 | 287.6 | 324.6 | +12.9% |
| 독립 재측정 계열 | 287.4 | 324.6 | +12.9% |
| 실험 T | 287.590 | 324.620 | +12.9% |
| AE2 큰 펌웨어 | 288.7 | 323.7 | +12.1% |

기존 `287.6 → 323.6 = +12.1%`은 서로 다른 빌드의 비율과 원시값을 묶은 것이다.
문서에는 **+12.1~12.9%** 범위를 쓰고, 원시값을 제시할 때는 같은 빌드 쌍만 쓴다.
양보 세금이 구조적이며 양보 레지스터 수에 거의 무관하다는 결론은 변하지 않는다.

## 2. 08-16 역사값과 현재 아티팩트 재측정

`stitched.s`와 `mul256_flat.s`가 당시 측정 뒤 다시 생성됐고, 9~17차 로드맵에는 그 뒤의
재측정 기록이 없다. 현재 생성 아티팩트를 5회 재측정한 값은 서로 일치했지만 08-16 기록과
다음처럼 다르다.

| 항목 | 08-16 역사값 | 현재 아티팩트 5회 | 차이 |
|---|---:|---:|---:|
| mul256 직선형 단독 | 397 | 377~378 | −19~−20 |
| θ 단독 / 곱×θ 스티치 | 88 / 413 (82%) | 70 / 396 (74%) | −18 / −17 |
| 라운드 v4 단독 / 곱×라운드 | 678 / 799 | 659 / 781 | −19 / −18 |
| round6 단독 / mul6 | 416 / 395 | 397 / 395 | −19 / 0 |
| 곱×round6 스티치 v6 | 681 (33%) | 662.35 (34%) | −18.65 |
| k=2 순차 / 스티치 | 1,206 / 1,019 | 1,186 / 1,001 | −18 |
| B-full 순차 / 솔버 | 727 / 657 | 726 / 656 | −1 |

처리 방침은 다음과 같다.

- 실험 5의 `A 유휴 슬롯 231 중 200 = 87% 소진`은 08-16 역사 계산으로만 남긴다.
  현재 값에서는 A 유휴 슬롯이 `2×377.5−565=190`인데 절감은 약 256cyc여서 A만을
  분모로 한 소진율이 100%를 넘는다. 이는 B의 유휴 슬롯도 A를 흡수했다는 뜻이며,
  **A 단독 슬롯 회계의 세 번째 검증**이라는 기존 해석은 철회한다.
- AF1에서 현재 생성 B 644명령과 솔버 입력 `exp_b.s`의 B 644명령이 완전 일치함을
  확인했다. 동일 ELF 독립 5회에서 수동 v6 **662.35**, B-full 솔버 **656.011**로
  정확히 반복됐고 차이는 **0.957%**다. 사전등록 ±1% 안이므로 현재 세대 판정은
  **동률**이다. `681→657(−3.5%)`은 08-16 역사값으로만 남긴다.
- F3의 `799 → 681`은 08-16 계보, 현재 아티팩트의 대응값은 `781 → 658~662`다.
  서로 다른 계보의 값을 한 화살표 안에 섞지 않는다.
- 역사 최강 순차의 pqm4 라운드 환산은 `229 → 230.25cyc`이므로 `397+230.25≈627`이다.
  `681/627=1.09배`로 결론은 변하지 않는다.

## 3. 닫힌 확인 항목

- **실험 Q 노출′ 113cyc 유지:** `zip′−B′raw = 868−754 = 114cyc`이고, 이는
  `113cyc + 검산 오차 1cyc`와 맞는다. 117cyc는 루프 포함 zip′과 루프 제외 B′를
  섞은 계산 오류다.
- **T1 측정 루틴 구분:** 같은 run에 루프 평균 루틴과 N=100 중앙값 루틴이 나란히 있다.
  Keccak은 `x1000 → 5,504`와 `N=100 → 5,526`이 5회 정확히 반복됐고, X25519는
  `x10 → 357,507~357,511`과 `N=100 → 357,475~357,500`이다. 따라서 5,504와
  357,510을 “다른 회차”로 본 설명은 폐기한다. 08-16의 정확한 357,474 원 로그는
  보존되지 않았으며 현재 재측정은 그보다 +1~+26cyc(최대 0.007%)다.
- **T6/T7 대조군 표류 아님:** 951/947과 1,772/1,770은 각 실험의 같은 펌웨어 안에서
  잡은 대조군이다. 차이 0.1~0.4%는 사전등록 ±1% 안이므로 오류가 아니며 펌웨어만 병기한다.

## 4. 재현 잡음으로 분류한 항목

- 실험 3 A-세금은 역사값 880/861(−2.2%)과 현재 848~850 / 849~860
  (−0.1~+1.5%)의 부호가 일치하지 않는다. 유지할 결론은 **약 0**뿐이다.
- 실험 K의 순차/굵은 교대는 3,206/3,219 · 3,230/3,202 · 3,203/3,200으로
  run마다 부호가 바뀐다(±0.9%). **굵은 교대≈순차**와 스티칭 −8.1~−8.7%는 견고하다.
- K3 재측정은 +0.03~+0.3%, 결합기는 7,648→7,791~7,804(+2%)다. K4 체크리스트의
  ±200cyc 안이며 D·상한·절감률 변화는 0.3% 이하라 결론은 변하지 않는다.

## 5. 해소된 항목과 남은 경계

- AF2에서 실제 융합 배치는 연산마다 3개이고 dummy step은 0임을 계수했다. 동일작업
  `P8−2P4`는 code flash에서 114.35~114.75k/batch였으나 ITCM에서
  5.96~6.02k/batch로 약 95% 줄었다. 3배 감소 약 325k가 기존 미설명 약 332k와
  일치한다. **큰 U8 instruction working set(13.3 KiB)을 flash에 둔 배치 오류**가
  주원인으로 확정됐다. U8만 ITCM으로 옮긴 추가 단일변수에서도 U4는 flash 비용을
  유지한 채 `P8−2P4=−3.56~−3.23k/batch`가 되어 2-job 고유 추가 손실이 사라졌고,
  encaps 손실은 keygen의 약 2배로 수렴했다. ITCM에 바로잡은 대표 all-U 종단간
  `F→U`는 −1.98/−3.72/−1.88%로 여전히 실패다. 남은 전체 구현 경계 비용을
  pipeline/setup/state traffic으로 세분하는 것은 미측정이다.
- J 은닉률은 래퍼 루프를 뺀 동일 펌웨어 실측 `A=309`, `B=752`, `절감=86`을 사용해
  **27.8%**로 통일한다. 28.1%는 A=310·절감=87의 반올림/경계 혼합 기록이다.

## 논문 반영 자격

- 바로 사용 가능: T2 `1,013→996, −1.7%, 58→60%`, yield2 세금
  `+12.1~12.9%`, J `27.8%`, Q 노출′ `113cyc`, 재현 잡음 범위.
- 역사값으로만 사용: 08-16 실험 4~7의 397/678/799/681/1,019 계열.
- 바로 사용 가능 추가: B-full 현재 세대 **±1% 동률**, AE2 U8 추가 손실의 flash 배치
  원인(`U8-only P8−2P4=−3.56~−3.23k`)과 ITCM 교정 종단간
  **−1.98/−3.72/−1.88%**.
- 사용 금지: 실험 5의 87% 슬롯 회계 검증, flash 배치 AE2 값을 U의 본질적 종단간
  성능으로 인용하는 것.
