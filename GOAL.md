# /goal — A주제 목표 설정

> 상태는 `XWing_CortexM85_통합_variation.md`가 단일 진실이며, 여기엔 **목표와 판정
> 규칙만** 둔다. 최종 갱신 2026-08-27.

---

## 0. 바로 붙여넣을 `/goal` 문구

`/goal`은 "언제 멈춰도 되는가"를 판정하는 **검사 가능한 종료 조건**이다. 목표 서술이
아니라 **완료 판정문**으로 써야 한다. 아래에서 지금 필요한 범위를 골라 쓴다.

### A. 실험 하나 (가장 자주 쓰는 형태)

```
/goal <실험ID>가 사전등록 gate로 판정될 것 — 통과면 채택하고 실패면 종료하며,
어느 쪽이든 결과 문서·아티팩트 동결·장부 갱신까지 끝낼 것. gate는 사후에 낮추지 않는다.
```

예: `/goal BB-2가 사전등록 gate로 판정될 것 — 통과면 채택하고 실패면 종료하며, …`

### B. 한 세션 분량

```
/goal XWing_CortexM85_통합_variation.md 기준으로 다음 우선순위 후보를 순서대로 진행해,
각각 사전등록 → 측정(독립 2회, ABBA) → 결과 문서 → 아티팩트 동결 → 장부 갱신까지 마칠 것.
투영과 실측을 섞지 말고, 미측정은 미측정으로 표기할 것.
```

### C. 프로젝트 종료 조건 (§7과 같음)

```
/goal §6 우선순위 1~5가 각각 채택 또는 사전등록된 gate로 종료되고,
draft-10 준거 baseline에서 대표값이 재측정되고,
채택 후보 전부가 §4를 통과해 아티팩트가 동결되고,
논문용 수치가 투영/실측으로 구분되어 정리될 것.
```

### 쓰지 말아야 할 형태

- ❌ "X-Wing을 최적화한다" — 종료 판정이 불가능하다
- ❌ "encaps를 10% 줄인다" — **측정 전에 결과를 고정**하는 것이라 §3-3 위반이다
  (임계값은 gate로 사전등록하되, 목표는 "gate로 판정될 것"이어야 한다)
- ❌ "가능한 한 빠르게" — 검사 불가

## 1. 목표 (한 문장)

**Cortex-M85(EK-RA8M1)에서 X-Wing(ML-KEM-768 + X25519 + SHA3-256)의 keygen/encaps/decaps
latency를, 프로토콜 정의를 바꾸지 않고, 사전등록된 kill gate를 통과한 최적화만 누적해
줄인다.**

## 2. 대상과 경계

**대상**: `XWing_CortexM85_통합_variation.md`의 71개 variation 가족(X/K/W/T 계열)과
§10의 실험 E0~E11.

**바꾸면 목표 이탈** (장부 §2):
- ML-KEM-768 → 512/1024 교체, X25519 → 다른 곡선
- combiner 입력·label·순서 변경
- encapsulation-key check / decapsulation reject / ciphertext 비교 생략
- 비밀 의존 branch·주소·table index 추가
- 여러 요청 throughput을 단일 latency로 보고

## 3. 작업 규칙 (AGENTS.md 확장)

1. **측정 우선.** 보드·컴파일러·인코딩·정확성·사이클처럼 직접 확인 가능한 것은
   실제 빌드·KAT·실보드 측정으로 결론낸다.
2. **미측정은 미측정이라고 쓴다.** `가정`/`추정`/`미측정` 표기 필수. 투영을 절감률로
   인용하지 않는다.
3. **사전등록 먼저.** 임계값은 **측정 전에** 고정하고 사후에 낮추지 않는다.
   못 넘으면 종료하고, 재도전은 새 사전등록 + 새 임계값.
4. **부정 결과도 결과다.** 실패·이탈·덮어쓴 로그·틀린 예측을 결과 문서에 남긴다.
5. **모델은 baseline을 재현해야 쓴다.** 예측이 실측 baseline과 어긋나면 그 모델로
   만든 스케줄을 보드에 올리지 않는다.
6. **독립 2회 flash/run + ABBA + DWT 중앙값.** 배치 교란은 통제하고, 통제되지 않으면
   성능 판정을 하지 않는다.
7. **같은 ELF 안에서 A/B.** 다른 빌드의 수치를 이어 붙인 것은 **파생 수치**로만 표기.
8. **계측이 대상을 바꾸면 그 빌드의 총합을 분모로 쓰지 않는다.** probe 가 붙은 빌드는
   비중 계산에만 쓰고, 절대 cycle 정본은 깨끗한 빌드에서 가져온다.
9. **손 asm 은 재정렬만 한다.** 레지스터 재할당은 커스텀 ABI 를 깨므로 금지.
   명령 수가 바뀌면 도구 버그로 본다.

## 4. 채택 조건 (모든 후보 공통)

| | 조건 |
|---|---|
| 정확성 | RFC 7748 KAT · FIPS 203 KAT · SHA3 KAT · full X-Wing 8-seed 바이트 동일 + 암묵적 거부 동일 · 기존 하네스 mismatch 0 |
| 상수시간 | 비밀 의존 분기·주소 금지, all-zero/all-ones 실행시간 분포 겹침 |
| 성능 | full X-Wing 세 연산 중 **하나라도 회귀하면 채택 금지**. 적용 지점이 없는 연산은 ±0.3% 이내(교란 통제) |
| 자원 | `.text`·표 크기·ITCM working set·stack high-water 보고. 스필 발생은 실패 신호 |
| 재현 | ELF/SREC/로그 SHA-256 동결, 생성기 결정성 |

## 5. 현재 좌표 (2026-08-26)

- **X-Wing 전 구간 분해 완료** — 미측정 블록 0
  (encaps 기준: X25519 mul 26.1% / sqr 22.5% / Keccak 22.4% / X25519 add·sub 10.5% /
  matacc 8.0% / 나머지)
- **채택 완료**: X/Y NTT, C8 store fusion, C9 MVE packing, **X06 fixed-base comb**,
  **X02 `fe25519_sqr` 재스케줄**(통합 +0.61/+2.17/+2.23%), **K31 libc 워드 교체**(+1.64/+0.92/+1.11%)
- **대표값(2024/039 정의, expBN)**: keygen 621,106 / encaps 977,896 / decaps 785,540
- **대표값(draft-10 정의, expBU)**: keygen **627,069** / encaps **988,456** / decaps **780,020**(warm) · **1,406,887**(cold)
  (expAL cell-00 대비 keygen −21.97% / encaps −17.41% / decaps −10.48%, 파생)
- **최종 누적 동일-ELF 실측(expCJ, draft-10)**: keygen **−25.967%** / encaps **−20.249%** /
  decaps **−11.224%**(warm) · **−18.298%**(cold). 독립 2회 ABBA·정확성·재현성 gate PASS.
  디스패처가 붙은 A/B이므로 **누적률만 정본**, 절대 cycle은 expBU가 정본이다.
- **libc 비용 실측**: newlib-nano `memcpy` 12.61 cyc/byte, `memset` 3.07 cyc/byte
- **M85 발행 규칙 확정**(expBA+expBL): `umaal` 1/cyc, ALU·load 는 곱셈과 100% 페어링,
  **store·`bfi` 는 페어링 불가**, **shifted-operand 는 공짜**. X01/X02 하한 회계의 근거다.
- **micro→full 감쇠 70%** — 표본 2개(X02 sqr, X01 mul)에서 일치. 원인 미규명.
- **스티칭 실증 완료**(expCA~CE): 병합 커널이 양쪽 출력을 정확히 내고(mm 0), 스필 0, 은닉률 60~85%.
  누출 법칙 **`C−A ≈ 0.34 cyc × 스칼라 명령 수`**(종류 무관). 겹침 규칙은 **MVE 저장만 벽**.

## 6. 후보 가족과 처리 상태 (**목록은 고정. 닫힌 것도 지운다 대신 표시한다**)

> 2026-08-26 정정: 이전 판은 **열린 항목만** 남기고 닫힌 것을 지웠다. 그러면 §7-1
> ("§6의 1~5번이 종료될 것")이 **영원히 닫히지 않는 이동 표적**이 된다. 목록을 고정하고
> 처리 상태를 열로 둔다. 진행도는 이 표에서만 센다.

| # | 가족 | 분모 (실측) | 상태 |
|---|---|---|---|
| 1 | **X01/X02** mul·sqr 재스케줄 | encaps 36.7% / decaps 34.0% | **완결** — X02 채택(expBC), **X01 채택(expBM 평탄화 +6.37% → expBN 통합 encaps +1.16%)** |
| 2 | **X06** fixed-base comb | keygen/encaps pkX·ctX | **완결** — 채택(expAX/AT-3) |
| 3 | **K19** scalar Keccak 재스케줄 | permutation 5,504 cyc | **종료**(expBE, 여지 4.6%) |
| 4 | **K22** absorb 캐시 | absorb 11.4 kcyc | **종료**(expBF/BG, gate 2회 미달) |
| 5 | **K31** libc `memcpy`/`memset` | 호출 18~19회/연산 | **완결** — 채택(expBH) |
| 6 | **W11/W12** combiner | 상한 0.22~0.31% | **종료** |
| 7 | **W16** 최상위 arena / **K30** verify·cmov | 25~32 cyc / 0.03% | **종료** |
| 8 | **X08/X09** MVE field arithmetic | — | **종료**(AM0/AM1 기각) |
| 9 | **K24** squeeze→CBD 융합 | 배관 950 cyc 중 **93.2%가 버퍼가 아니라 비트-인터리브 해제**(expBJ) | **표적 오류 — 상한 배관의 10% 미만(추정)** |
| 9b | **K32** `StateExtractBytes` 교차 배치 | 885.81 cyc x 28회 = 24.8 kcyc/연산 | **종료**(expBK): micro +17.57% < 20%, full 0.41~0.69% |
| 10 | **K20/K21** Keccak 배칭 구조 | Keccak 평균 29.0% | **둘 다 종료**: K21 은 보드 실측 −2.0~−3.0%(expBS, dummy lane 이 18.13% 를 삼킴), K20 은 스칼라 회전이 공짜(expBL)라 MVE 가 회전에 3배 명령이 든다(expBT) |
| 11 | **K14/K15** matacc rejection+MAC 융합 | 약 50.6 kcyc/연산 | **종료(expBX)**: 여유 17.6%(2.14% 아님 — expBR 정정)이나 루프가 rejection 분기로 **9개 기본블록**으로 조각나 회수 평균 **0.20%** |
| 12 | **X03**(+X05 흡수) 독립 mul/sqr 쌍 융합 | mul+sqr = encaps 48.6% | **종료**(expBQ): `umaal` 이 **의존 상태에서도 1.000 cyc**(expBA)라 교차로 얻을 것이 없다. 융합 상한 평균 **0.8%**, `fe25519_addsub` 도 **0.39%** |
| 13 | **K07** keygen domain-flow | invNTT 11,882 + NTT 몫 | **종료**(expBP §3): 평균 상한 ≤1.12%, 출력 바이트 동일 증명 필요 |
| 14 | **K28/K29** decaps pipeline | K28 ≤10,789 / K29 약 50~55 k(추정) | **둘 다 종료**(expBP §2,§6): K28 평균 상한 ≤0.46%, K29 는 계산량이 줄지 않는 **표적 오류** → 분모를 W04/W05 로 이관 |
| 15 | **W03/W04/W05** X25519×ML-KEM 교차 스케줄 | 합성 스트림으로는 은닉률 60~85%(expCA~CE). **실물 `fe25519_sqr`×NTT 병합은 정확하지만 순차보다 느리다** — 318.12 vs 280.25 cyc, 사전등록 gate 6개 전부 FAIL (expCG/CH/CI) | **종료.** 재물질화가 숨지 않는다: `movw` 듀얼이슈 불가(0.99 cyc), `ldr` 은 MVE 메모리 명령 옆이라 0% 겹침. 빈 레지스터 평균 1.6개로는 상한도 35% (T25.4) |
| 16a | **W10** true x2 Keccak | 겹칠 대상 = combiner **2,537 cyc** | **종료**(expBP §1): 평균 상한 **0.194%** |
| 16b | **W14** cold·warm API 분리 | cold = warm × **1.804** (expBU) | **종료** — 최적화 후보가 아니라 **보고 규약** 문제. cold 비용은 정의가 요구하는 keygen 자체 |
| 17 | **E0** draft-10 준거 이동 | — | **완료**(expBU+expBW): 네 항목 동시 이동 + 대표값 재측정 + **공식 test vector `ss` 완전 일치** |
| 18 | **T-계열** M85 발행 규칙 실측 | — | **완결** — expBA(umaal/ALU/ldr/str) + expBL(shifted/bfi) |

**닫힌 것 20 / 20 가족.**
**남은 것: 없다 — 20/20 가족이 닫혔다(2026-08-27).** 마지막이 W03/W04/W05 였다: 죽은 호스트 레지스터를 빌리는 할당기를 만들어 채택본 `fe25519_sqr` 에 NTT 를 끼웠고 **1,007 벡터에서 정확했으나 병합이 순차보다 느렸다**(318.12 vs 280.25 cyc, gate 6개 전부 FAIL — T25). K14/K15 는 expBX 로, K24 는 expBJ 로 이미 닫혀 있었다(이 요약줄이 그동안 갱신되지 않았다). **E0 은 완료**(공식 KAT `ss` 일치). W07 U 스티칭, AC x4 Keccak 통합, C9 v2 scatter 도 종료됐다.

**닫힌 방식의 분포**: 채택 4(X01·X02·X06·K31) / 완료 2(E0 draft-10, M85 발행 규칙) / 사전등록 gate·상한으로 종료 14. **부정 결과가 다수인 것이 이 목표의 결과다.**
(별도 가족 번호 없음).

## 7. 이 목표가 끝났다고 말할 수 있는 조건

1. §6 표의 **모든 가족이 채택 또는 사전등록된 gate로 종료**될 것 — **완료(2026-08-27): 20/20**
   (진행도(%)는 **§6 표의 닫힌 가족 수 / 전체 가족 수**로만 센다. 다른 축으로 센 숫자를
   진행도로 보고하지 않는다.)
2. draft-10 준거 baseline에서 대표값과 최종 누적률이 재측정될 것 — **완료**
   (절대값 expBU §3, 동일-ELF 누적률 expCJ)
3. 모든 채택 후보가 §4를 통과하고 아티팩트가 동결될 것 — **완료**
4. 논문에 쓸 수치가 **투영과 실측으로 구분되어** 정리될 것 — **완료**: `논문_데이터_표.md` T17~T25 (실측만 본문, 파생·투영은 표기 분리, T23 은 정정 이력, T25.5 는 누출 법칙의 적용 한계)
