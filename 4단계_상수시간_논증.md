# 상수시간 논증 — 스티칭은 타이밍 부채널을 추가하지 않는다 (4단계, 2026-08-17)

## 주장

스티치드 커널(지퍼·SLOTHY 솔버 산출물 공통)의 실행 사이클 수는 비밀 입력과 무관하다.
스티칭이 원본 대비 **추가하는** 타이밍 누설은 0이다.

## 논증 (구조적)

스티칭 = **컴파일 타임 정적 스케줄 변환**. 산출물 asm은 입력과 무관하게 고정이며,
변환이 하는 일은 다음 세 가지뿐이다:

1. **명령 순서 재배열** — 제어 흐름 추가 없음(분기 0개 삽입). 인오더 코어에서 정적
   명령 시퀀스의 사이클 수는 데이터 값에 의존하지 않는다(아래 전제 참조).
2. **레지스터 재할당** — 값의 위치만 바뀜, 타이밍 무관.
3. **스필 삽입** (컴파일러 `-ffixed` 경유) — 스필 주소는 **고정 오프셋**(sp/전역 상대
   상수). 비밀 의존 주소 접근이 없으므로 캐시/버스 타이밍이 입력과 무관. 우리 배치는
   DTCM(0-wait, 캐시 없음)이라 이 여지 자체가 없다.

### 스트림별 전제 (원본의 상수시간성 — 스티칭은 이를 보존만 함)

- **A(X25519 필드 산술)**: fiat-crypto 생성 코드 — 비밀 의존 분기·비밀 의존 메모리
  주소 없음(fiat의 설계 보증). Lenngren asm도 동일 성질(래더는 cswap 상수시간 구현).
- **B(Keccak 순열)**: 데이터 무관 고정 시퀀스(순열은 애초에 입력별 분기가 없음).
  MVE 4-way판도 고정 오프셋 vldrw/vstrw + 논리/회전 연산뿐.
- **글루(4-way 큐)**: 배치 크기·순열 횟수는 연산별 상수(ML-KEM-768: 43/44/44회) —
  스케줄링 타이밍이 비밀에 의존하지 않음.
- **M85 전제**: 정수·MVE 산술 명령의 레이턴시는 오퍼랜드 값과 무관(조기 종료 곱셈기
  없음 — ARMv8.1-M). DTCM 접근 0-wait 고정.

## 실증 (실리콘, EK-RA8M1)

1. **입력 2벌 교차 측정** (하네스 슬롯 50 vs 58): expj_stitch(진짜 fiat 곱 × MVE 라운드
   스티치)를 전혀 다른 입력 두 벌로 N=100 측정 —
   **median Δ = 0 cyc** (979,065 vs 979,065; x1000 반복이라 분해능 0.001 cyc/iter 수준).
2. **결정성**: 전 슬롯에서 min ≈ median (잔여 jitter는 수 cyc 수준의 인터럽트/버스
   노이즈로, 입력과 무관하며 median에서 소거됨).
3. 재현: `harvest_ra8m1.py` 출력의 `[상수시간]` 행 — 자동 PASS/FAIL 판정.

## 한계 (정직 고지)

- 실증은 대표 커널(expj_stitch) 1종 — 전 연산 확대 시 X-Wing 3연산 각각에 같은
  2벌 테스트를 배선하면 됨(비용: 슬롯당 측정 1회).
- dudect류 통계 검정까지는 안 함 — 인오더+TCM 환경에서 값-의존 타이밍의 물리적
  경로가 없다는 구조 논증이 1차이고, 2벌 측정은 그 확인 사살.
- 전력 부채널(DPA)은 범위 밖 — 본 논증은 타이밍에 한정.
