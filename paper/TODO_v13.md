# TODO v13 — `리뷰_v12_상위학회.md` 적용 (오푸스 실행용)

작성 2026-08-28 (페이블). 각 항목은 독립적으로 실행 가능하고, 완료 판정 기준을 적었다.
원칙: 편집 전 `ko/main.tex.bak-20260828d` 백업. md/tex는 Write/Edit 도구로만(PowerShell 재작성 금지).
끝나면 `.\build.ps1 ko` → error/undefined/overfull 0 확인 → `.\package_overleaf.ps1`.
보드·솔버 실행이 필요한 항목은 **[보드]**로 표시했고 그 외는 문서 작업만이다.

---

## A. P0 — 재제출 전 필수

### A1. `tab:loo` 산술 통일 (P0-1)
- [ ] 원시 로그 `measure-harness/logs/2026-08-28_expCUWX_run{1..4}.txt`에서 run별 $B_{-i}-B$ 네 paired 값과 $A-B$를 다시 뽑는다.
- [ ] 추정량을 **하나**로 정한다. 권장: 각 run에서 "네 paired의 중앙값"을 셀로 두고, 합·잔차는 **그 셀들을 직접 더한 값**으로 계산한 뒤 run 간 범위(min–max)를 보고. 대안: 셀은 현행 min-of-min 유지, 합·잔차 줄만 셀에서 직접 더한 값으로(잔차 = 19,454 / 19,481 / 391 / 19,289).
- [ ] 세 문서를 같은 숫자로: `ko/main.tex` tab:loo, `measure-harness/2026-08-28_expCU_CV_CW_CX_results.md` CU 표, `논문_데이터_표.md` expCU 표.
- [ ] tab:loo 캡션에 추정량을 정확히 한 문장으로("셀·합·잔차 모두 …로 계산"). "두 run 중" → "네 run 중".
- 완료 기준: 표의 여섯 셀 합 = "합" 줄, A−B − 합 = "상호작용" 줄이 네 열 모두에서 정확히 성립.

### A2. expCW 범위 통일 (P0-2)
- [ ] `논문_데이터_표.md` T34(현 번호)를 4 run × 4 pair 범위로: A warm 1,010,686–1,010,864 / B 879,160–879,676 / g **12.97–13.02%**; cold A 1,848,534–1,848,948 / B 1,504,314–1,505,130 / g **18.59–18.62%**.
- [ ] `ko/main.tex` l.344 "warm 12.97--12.98\%, cold 18.59--18.60\%" → "warm 12.97--13.02\%, cold 18.59--18.62\%".
- 완료 기준: grep "12.97" 결과가 원고·데이터표·결과문서·README에서 모두 "13.02"와 짝.

### A3. 데이터표 T33 중복 (P0-3)
- [ ] `논문_데이터_표.md`: 1107행 "## T33 — draft-10 leave-one-out" → **T34**, 이하 T34→T35(expCW), T35→T36(expCX), T36→T37(expCT).
- [ ] 로드맵 70차·README·results 문서에서 "T32–T35" 같은 번호 참조가 있으면 갱신.
- 완료 기준: `grep -n "^## T3[3-7]"`에 번호 중복 없음.

---

## B. P1 — 사실 오류·불일치 (각 한 줄)

| # | 파일:위치 | 현재 | 수정 |
|---|---|---|---|
| B1 | main.tex l.766 | "두 조각을 모두 재스케줄한 후보" | "나머지 세 조각을 재스케줄한 후보" |
| B2 | main.tex l.498 | "상류 이슈 초안 세 건" | "상류 이슈 초안 다섯 건" |
| B3 | main.tex l.89 | "(IPC 1.66)" | "($I/C$ 1.66)" |
| B4 | main.tex l.844 | "명령 증가나 IPC 변화에" | "명령 증가나 $I/C$ 변화에" |
| B5 | main.tex l.518–519 | "각 창은 모두 OPTIMAL로 끝났지만" | "최종 패스의 창은 모두 OPTIMAL이었지만 seed 2의 한 창은 첫 패스에서 300초 FEASIBLE로 끝났고" |
| B6 | main.tex l.1038 | "스무 값의 폭은 0.006--0.013\%p" | "0.005--0.013\%p" |
| B7 | main.tex tab:final 캡션 (l.994–997) | "독립 다섯 번 flash한 A-B-B-A 스무 값의 범위" | 끝에 "아래 표의 F/U는 같은 다섯 flash, S/X는 독립 두 flash의 범위다" 추가 |
| B8 | main.tex l.1204–1210 재현 아티팩트 | CO/CP/CQ까지 | "Lenngren r11 양보(CR), 조각 솔버 재스케줄(CS: 폐기 run의 로그 포함), seed 스윕(CT), leave-one-out(CU), 5-run(CV), 8-seed 순환(CW), 고정/무작위 ct 검정(CX: 결함 run 1–4 로그 포함)" 추가 |
| B9 | main.tex tab:loo 캡션 (l.1313–1315) | "두 run 중 작은 쪽" | A1에서 정한 추정량 문장으로 교체 |
| B10 | main.tex 결론 (l.1153 뒤) | CS·CU 언급 없음 | 두 문장 추가: "융합 조각 하나를 솔버로 다시 짜도 지퍼보다 654--734 cycle 느렸다(실험 CS)." / "비스티칭 대안의 leave-one-out에서는 fixed-base comb 하나가 키생성·캡슐화·cold 감소의 약 2/3를 차지했다(\cref{tab:loo})." |
| B11 | main.tex l.1286 (부록 A-B-B-A) | "독립 반복 수는 두 번이고" | "독립 반복 수는 두 번이며(주 표 F--U만 다섯 번, \cref{sec:bounds})" |

- 완료 기준: 각 행을 grep으로 확인.

---

## C. P1 — 방법론 한 줄씩

- [ ] **C1** (P1-2) 계측 통제 문단(l.954 부근) 끝에 추가: "실험 CS에서 내용이 같은 코드가 flash 주소만 달라 718 cycle(구간의 0.8\%) 차이를 냈다. 같은 크기의 주소 효과가 flash에 둔 U의 in-context 측정에도 있을 수 있으나, 융합 손실 $+219$k의 0.3\%로 은닉률의 부호나 크기에 영향을 주지 않는다."
- [ ] **C2** (P1-3) objdump로 `fiat_mul_u4t_p0` 본문의 스칼라 명령 453개 중 r0/r1/r10/r12만 오퍼랜드로 쓰는 명령 수와, r2–r9 중 하나라도 쓰는 명령 수를 센다. 결과를 l.760 "여기서 쓴 솔버는 상류의 온전한 능력이 아니다(재정렬은 제약 없음)" 뒤에 한 문장으로: "스칼라 명령 453개 중 N개(P\%)가 r2--r9를 쓰므로 재할당은 사실상 그 나머지에서만 열려 있었다." 숫자는 측정값으로.
  - 스크립트: `measure-harness/artifacts/2026-08-28_expCS/expCS_solver.S`의 `fiat_mul_u4t_p0` 본문을 정규식으로 세면 된다(보드 불필요).
- [ ] **C3** (P1-5) 한계 절에 한 문장: "고정/무작위 ct 검정은 클래스당 3,000표본이며 B-warm의 $|t|=1.99$는 임계에 가까우므로, 표본을 10배로 늘렸을 때의 결과는 미측정이다."
- [ ] **C4** (P1-4) tab:loo 아래 문단(l.1331–1334)에 한 문장: "상호작용이 fixed-base가 켜진 세 연산에서만 거의 같은 크기로 나타나므로 fixed-base와 한 축 사이의 2-way 상호작용이 후보이며, base 곱셈 경로를 함께 지나는 k31(\texttt{memcpy})과 x02(제곱)가 우선 후보다. 두 축 동시 off는 재지 않았다."

---

## D. P2 — 분량·서술 (학회 분량 상한이 있으면 필수)

- [ ] **D1** 초록 숫자 축소: 주장당 하나. 15--29\% / 1.2--1.7\% / "532k, 그러나 손으로 양보하면 −3.9\%" / "솔버로 다시 짜도 지퍼보다 느렸다". 654--734, 157.0→150.9, 36--49\%, 6.6--8.0\% 같은 범위는 본문으로.
- [ ] **D2** §6.1 순서: (f) 문단을 (d–e) 뒤로 옮기거나 이름을 (b′)로. 표 5 행 순서와 일치시킬 것.
- [ ] **D3** l.1021–1031("순차 기준 S와 Fiat 교체 비용")과 l.1049–1062("스티칭 계열의 판정")의 중복 제거 — 532k·CR·"지퍼 탓 아님"은 판정 문단에만.
- [ ] **D4** 분량 28→22쪽:
  - §5.2 "솔버에 필요했던 것" 항목 5개 → 2열 표(추가 항목 / 강제한 실측 실패).
  - §6.1 (a)–(f) 여섯 문단 → 표 5 + 문단 두 개(스칼라 계획의 자기잠식 / 양보 세금은 컴파일러의 것).
  - 부록 B "준거 이동과 API 프로파일"·"한 번에 토글한 채택 스택" 두 소절 → 각 3문장으로, 나머지는 아티팩트.
  - 측정 세부사항 부록의 SHA-256 나열 → "MANIFEST.sha256 참조"로.
  - 한계 문단 → 항목화(itemize), 중복 제거.
- [ ] **D5** 빌드 후 쪽수·overfull 확인. 22–23쪽 목표.

---

## E. 문서 동기화 (마지막)

- [ ] main.tex 헤더 주석에 v13 줄 추가(무엇을 고쳤는지 한 줄).
- [ ] `paper/README.md` 현재 상태 표: v13, 쪽수.
- [ ] `PQC_스티칭_연구_로드맵.md` 변경 이력 71차 + "이번 주 할 일" 갱신.
- [ ] `리뷰_v12_상위학회.md` 끝에 "§8 반영 결과" 표(항목별 반영/미반영).
- [ ] `.\package_overleaf.ps1` 실행, ZIP 갱신.

---

## F. 선택 [보드]

- [x] **F1** (P1-1, 2026-08-28 완료 — expCY: keygen 25.97→25.96%, 나머지 불변) `tab:cumulative` 5-run: expCJ 동결 이미지를 독립 5회 flash(CV와 같은 절차). 범위가 기존 2-run을 벗어나면 표 갱신, 아니면 "독립 다섯 번"으로 표기만.
- [x] **F2** (완료 — 8 seed 반영·폴더 개명 끝) expCT seed 23·101 결과가 나왔으면 "여섯 seed" → "여덟 seed"로, 82/83 횟수 갱신. `artifacts/2026-08-28_expCR_seed_sweep/` → `expCT_seed_sweep/` 개명.
- [ ] **F3** 상류 SLOTHY 이슈 4·5 제출 — 사용자 GitHub 계정 필요, **사용자 결정**.
