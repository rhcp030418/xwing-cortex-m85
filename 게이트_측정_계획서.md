# 1단계 킬 게이트 — 측정 계획서 (사전 등록)

> **v0.1 초안, 2026-08-10.** 보드 도착 전에 판정 기준과 절차를 고정해두는 문서.
> **이 문서 이후 기준·공식 변경은 로드맵 "변경 이력"에 사유와 함께 기록해야만 유효하다.**
> 결과를 보고 나서 기준을 옮기는 것을 원천 봉쇄하는 것이 이 문서의 존재 이유다.

---

## 정오 — 2026-08-19 (기준값 이동 없음)

**본문은 원문 그대로 둔다.** 사전 등록 문서를 사후에 고쳐 쓰면 이 문서의 존재 이유가
사라지므로, 아래는 삭제·대체가 아니라 **읽는 법의 확정**이다. 세 항목 모두 임계값을
움직이지 않으며, 로드맵 변경 이력 2026-08-19에 기록되어 §머리말의 유효 요건을 갖춘다.

| # | 대상 | 확정된 읽는 법 | 사유 |
|---|---|---|---|
| E1 | §2 `≥ 30% 근처` | **`≥ 30%`** — "근처"는 무효 | §6-5가 "기계적 적용, 해석 여지 없음"을 규정하므로 재량 표현과 양립 불가. 임계값 30%는 불변 |
| E2 | §2 `15~30%` / `≥ 30%` 경계 중복 | **`15% ≤ x < 30%`** / **`x ≥ 30%`** | 정확히 30.0%가 두 칸에 걸렸음. 30%는 상위 칸(직진) 소속 |
| E3 | §2 `퇴로 A` | **듀얼코어 논문의 베이스라인으로 데이터 회수** | 표에 정의가 없어 단독 인용 시 의미 불명. §2 본문 괄호와 동일 내용 |

**적용 확인**: 1단계 실측 상한은 21.2~26.4%(평균 23.9%)로 E1~E3 어느 해석에서도
`15% ≤ x < 30%` 한 칸에만 떨어진다 — **판정 결과는 정오 이전과 동일**(파일럿).

## 정오 2 — 2026-08-20 (분모 정의 확정 · 판정 이동 있음, 자진 신고)

| # | 대상 | 확정된 읽는 법 | 사유 |
|---|---|---|---|
| E4 | §5 `상한% = 채택된 overlap / (cyc_A + cyc_B)` | **분모 = X-Wing 연산 1회 전체 사이클** — 즉 `(스칼라곱 횟수)×K1 + K3 + 결합기`. 스트림 B는 "ML-KEM 내부 Keccak"이 아니라 **ML-KEM 전체**로 읽는다 | 근거·전량 재계산: `분모_정의_통일.md` |

**E1~E3과 달리 이 항목은 판정을 옮긴다 — 그러므로 별도 블록으로 분리해 신고한다.**

§3이 K4를 `K1 + K3 + 결합기`로 정의한 것과, §1이 질문을 "**전체**의 몇 %인가"로 적은 것이
E4의 근거다. 반대로 리포트 §3이 스트림 B를 "ML-KEM 내부 Keccak(+결합기 순열)"로 정의한
채 §5 공식을 문자 그대로 적용하면 분모는 `K1 + Keccak부`가 되어 값이 달라진다:

| 연산 | 분모 = K1+Keccak부 | 상한 | 분모 = 전체(E4 확정) | 상한 |
|---|---|---|---|---|
| keygen | 595,092 | 35.3% | 794,198 | 26.4% |
| encaps | 963,618 | 25.8% | 1,175,479 | 21.2% |
| decaps | 606,144 | 34.6% | 868,772 | 24.2% |
| 평균 | — | **31.9% → §2 "직진"** | — | **23.9% → §2 "파일럿"** |

**즉 실제 판정(파일럿)은 문자 그대로의 공식이 가리키는 칸(직진)이 아니다.** 리포트 v1.0은
처음부터 전체 사이클 분모를 썼으나 그 선택을 명시하지 않았다. E4는 **사후 선택이 아니라
사후 명문화**이며, 방향은 **저자에게 불리한 쪽**(더 낮은 상한, 더 보수적인 칸)이다.

확정 근거: 논문이 주장하는 것은 **종단간** 이득이고, 종단간 주장의 분모는 종단간 연산이어야
한다. `K1+Keccak부` 분모는 스티칭이 손대지 못하는 ML-KEM 비-Keccak 구간(연산별 D의
17.8~30.0%)을 분모에서 숨겨 이득을 부풀린다.

**2차 기준(§"알려진 공백")에의 영향**: 리포트 §5-2의 `실회수 15%+`도 같은 분모로 읽는다.
정정 전(11.4%)·후(7.7%) 모두 15% 미달이므로 **2차 판정도 불변**(퇴로 B).

### 알려진 공백 — 2차 게이트(파일럿 재판정) 기준

**이 v0.1에는 파일럿 재판정 기준이 없다.** §2는 15~30%일 때 "2주 파일럿으로 축소해
재판정"까지만 서약하고, 재판정을 무엇으로 하는지는 등록되지 않았다.

실제 기준(`상한 26% 대비 실회수 15%+면 파일럿 통과 가치`)은 `1단계_게이트_리포트.md`
§5-2에 있고, 그 §5는 **v1.0(게이트 판정 시점, 파일럿 착수 전)** 작성분이다 — v1.1이
§5-1을 "이행"으로 참조하는 것이 시점 증거다. 따라서 사후 제정은 아니나, **1차 게이트와
같은 수준의 사전 등록은 아니다.** 논문에서 사전 등록을 방어할 때는 1차 게이트에 한정해
주장하고, 2차 기준은 "판정 시점에 리포트에 기재"로 약하게 서술할 것.

## 1. 질문

순차 하이브리드(X25519 → ML-KEM)의 실행 중, 명령어 교차로 회수할 수 있는
**낭비(빈 발행 슬롯 + 스톨)가 전체의 몇 %인가?** — 이득 상한 = 현재 낭비량.

## 2. 서약된 판정 기준 (로드맵과 동일, 재확인)

| 측정된 상한 | 행동 |
|---|---|
| < 15% | **중단.** 데이터는 퇴로 A(듀얼코어 논문의 베이스라인)로 회수 |
| 15~30% | 2단계를 2주 파일럿으로 축소해 재판정 |
| ≥ 30% 근처 | 직진 |

## 3. 측정 대상 (전부 M85 실보드, -mcpu=cortex-m85 재빌드)

| # | 커널 | 소스 (검증 상태) |
|---|---|---|
| K1 | X25519 스칼라곱 1회 | Lenngren asm — RFC 7748 KAT 통과(2026-08-10, QEMU) |
| K2 | Keccak-f1600 순열 1회 | pqm4 스칼라 asm — SHA3 KAT 통과(2026-08-10, QEMU) |
| K3 | ML-KEM-768 keygen/encaps/decaps 각각 | pqm4 (QEMU 기능 검증 완료) |
| K4 | 순차 하이브리드 합계 (K1+K3+결합기 SHA3-256) | K1~K3 조합 |

## 4. 측정 항목 (커널별)

1. **사이클 수**: DWT_CYCCNT 또는 Armv8.1-M PMU 사이클 카운터
   — ⚠ M85에서 어느 쪽이 동작하는지, PMU 이벤트 ID 목록은 **보드 도착 후 확인해 기입** (지금 단정 금지)
2. **명령 수**(retired instructions) → **IPC = 명령수/사이클**
3. 가능하면 스톨 사이클의 원인별 분해(프론트엔드/백엔드) — 이벤트 지원 여부는 보드에서 확정

## 5. 상한 계산 (1차 근사 공식 — 보수 원칙)

두 가지로 계산해 **작은 쪽을 채택**한다:

- **슬롯 회계 상한**: M85 듀얼이슈 기준 구간별 빈 슬롯
  `idle_K = 2×cyc_K − inst_K` (벡터 beat 회계 왜곡은 각주로 명시)
  → 상대 스트림이 채울 수 있는 양으로 환산
- **길이 균형 상한**: 두 스트림 중 짧은 쪽만큼만 겹칠 수 있음
  `overlap ≤ min(cyc_A, cyc_B)` — 연산별(keygen/encaps/decaps)로 각각 계산
  (2026/1414의 M4 수치 어림상 X25519 쪽이 김 → 커버리지 희석 예상, M85에서 실측)

`상한% = 채택된 overlap / (cyc_A + cyc_B)` — X-Wing 3연산 각각 + 가중 평균 보고.

## 6. 절차

1. 카운터 캘리브레이션: NOP 100개 루프로 측정 오버헤드 확인 (오버헤드 > 1%면 측정 루틴 수정)
2. 각 커널 N=100 반복, **중앙값** 채택 (2026/1414 방법론 준용), 최소/최대 병기
3. **정합성 앵커**: M4 대비 자릿수 비교 — X25519 1회가 2026/1414의 0.615M 사이클과 크게 어긋나는 배율이면(클록·ISA 차이로 설명 안 되는 수준) 측정 오류로 간주하고 원인 규명 먼저
4. 결과를 1쪽 리포트로: 수치 표 + 상한 계산 + 판정 + (탈락 시) 퇴로 A 인계 메모
5. 판정은 §2 표를 기계적으로 적용 — 해석 여지 없음

## 7. 알려진 위협 (미리 적어두는 반론)

- QEMU/시뮬레이터(Arm FVP 포함)는 사이클 부정확 → **게이트는 실보드에서만 유효**
- 벡터(Helium) 명령의 beat 실행 때문에 IPC 기반 슬롯 회계가 왜곡될 수 있음 → 스칼라 커널(K1, K2)은 왜곡 없음, K3만 각주 처리
- 캐시/플래시 대기 상태가 사이클을 오염 → 측정은 TCM 또는 0-wait SRAM 배치로, 배치를 리포트에 명시
