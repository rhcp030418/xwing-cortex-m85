/* mul256 v2: IT-free straight-line (column lo/hi split accumulation) */
#include <stdint.h>
void MULNAME(uint32_t r[16], const uint32_t a[8], const uint32_t b[8])
{
    uint64_t p, sumlo, sumhi, carry = 0;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[0] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[1] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[2] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[3] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[4] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[5] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[6] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[0] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[1] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[0]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[7] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[1] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[2] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[1]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[8] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[2] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[3] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[2]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[9] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[3] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[4] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[3]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[10] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[4] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[5] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[4]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[11] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[5] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[6] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[5]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[12] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[6] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    p = (uint64_t)a[7] * b[6]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[13] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    sumlo = 0; sumhi = 0;
    p = (uint64_t)a[7] * b[7]; sumlo += (uint32_t)p; sumhi += (uint32_t)(p >> 32);
    carry += sumlo;
    r[14] = (uint32_t)carry;
    carry = (carry >> 32) + sumhi;
    r[15] = (uint32_t)carry;
}